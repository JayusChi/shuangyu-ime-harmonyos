# 双羽输入法 0.1.0 客户试用建议与修改方案

## 1. 文档目的

本文整理双羽输入法 0.1.0 版本交付客户试用后的反馈，并将每条反馈转换为可开发、可测试、可验收的修改项。

执行优先级按客户要求确定：

1. 先完成键盘结构、按键语义和候选栏体验；
2. 再完成输入码展示、候选展示和简拼自定义；
3. 浮动候选窗涉及系统能力和跨应用兼容性，先验证能力，再进入正式开发。

本文只定义修改目标和实施步骤，不表示所有功能已在 0.1.0 中实现。涉及“双羽方案”的编码映射、分词规则和默认第二候选键，在正式开发前还需要以最终方案表或产品规则为准。

## 2. 总体优先级

| 优先级 | 修改项 | 类型 | 目标版本建议 |
| --- | --- | --- | --- |
| P0 | 全键盘结构标准化 | 键盘 | 下一体验版 |
| P0 | 隐藏键移出主键盘 | 键盘 | 下一体验版 |
| P0 | 中文键盘增加动态 Shift/分词键 | 键盘、输入状态机 | 下一体验版 |
| P0 | 中文键盘字母统一显示为大写 | 键盘 | 下一体验版 |
| P0 | 空闲状态栏不再留白 | 候选栏、状态栏 | 下一体验版 |
| P0 | 候选词后不显示全拼码 | 候选栏 | 下一体验版 |
| P1 | Pad/手机输入法切换键与底部操作区 | 键盘、系统集成 | 第二个体验版 |
| P1 | 非拼音方案支持按键选择第二候选 | 键盘、输入状态机 | 第二个体验版 |
| P1 | 输入码按两码切分展示 | 输入、引擎 | 第二个体验版 |
| P1 | 简拼结果可自定义并固定排序 | 输入、用户词库、设置 | 第二个体验版 |
| P1 | 预编辑文本明确显示“未上屏”状态 | 输入、兼容性 | 第二个体验版 |
| P2 | 候选跟随光标的浮动候选窗 | 系统能力、Pad、外接键盘 | 能力验证通过后 |

## 3. 键盘方面

### 3.1 全键盘结构标准化，参考主流系统键盘

#### 客户反馈

全键盘结构可参考小艺，尽量标准化。

#### 修改目标

将中文、英文全键盘统一为用户熟悉的 QWERTY 结构。字母区、Shift、删除、数字/符号切换、语言切换、空格和回车的位置保持稳定，切换中英文时尽量不让按键大幅跳动。

目标布局建议如下：

```text
Q W E R T Y U I O P
 A S D F G H J K L
↑/分词 Z X C V B N M  删除
123  中/英  [      空格      ]  #+=  回车
```

手机与 Pad 可以使用不同的底部辅助区，但三行字母区必须保持相同顺序和相近位置。不能直接复制其他产品的图标或视觉资产，只参考通用键位习惯。

#### 具体修改步骤

1. 以 `Stage10KeyboardLayouts.ets` 为唯一正式布局定义，清理或停止继续扩展旧版兼容布局，避免两套 QWERTY 定义产生差异。
2. 抽出三组公共布局结构：`phonePortrait`、`phoneLandscape`、`tablet`。三组结构共享字母顺序，只改变边距、键宽、辅助操作区和空格行。
3. 将第三行改为“Shift/分词键 + Z～M + 删除键”，Shift 固定在 Z 左侧，删除固定在 M 右侧。
4. 将常用切换键统一放在空格行：数字、语言、空格、符号、回车。隐藏键按 3.2 的方案移出主键盘。
5. 在 `KeyboardMetrics.ets` 中分别配置手机、横屏和 Pad 的按键高度、行间距、左右缩进及底部安全区，不在布局文件中写死多套尺寸。
6. 保持按键 ID 和动作稳定；视觉位置调整不应改变删除、空格、回车和语言切换的业务语义。
7. 补充布局快照或结构测试，检查每种设备形态下的键位顺序、按键数量、Shift 和删除位置。
8. 在真机上分别验证手机竖屏、手机横屏、Pad 竖屏、Pad 横屏，以及带底部手势条的场景。

#### 涉及模块

- `entry/src/main/ets/presentation/keyboard/layouts/Stage10KeyboardLayouts.ets`
- `entry/src/main/ets/presentation/keyboard/model/KeyboardLayoutSpec.ets`
- `entry/src/main/ets/presentation/keyboard/design/KeyboardMetrics.ets`
- `entry/src/main/ets/presentation/keyboard/KeyboardRootStage3.ets`
- `entry/src/test/Stage10.test.ets`
- `entry/src/test/Stage11.test.ets`

#### 验收标准

- 中文和英文均为标准 QWERTY 排列；
- Shift 位于 Z 左侧，删除位于 M 右侧；
- 中英文切换时三行字母键不发生明显跳位；
- 所有按键均在安全区内，横竖屏无裁切、重叠和误触；
- 主键盘中不再出现独立的隐藏键。

### 3.2 隐藏键不占主键盘，移动到状态栏

#### 客户反馈

隐藏键不占主键盘，放到状态栏上。

#### 当前问题

当前布局在中文、英文、数字、电话和符号键盘的底部行中都放置了 `hide` 键，占用了常用按键空间，并导致空格行较拥挤。

#### 修改目标

在键盘顶部状态栏最右侧固定显示“收起键盘”图标。主键盘的所有模式都删除 `hide` 键。候选较多时，收起图标仍必须可见、可点击，不能随候选内容一起横向滚出屏幕。

#### 具体修改步骤

1. 从 `Stage10KeyboardLayouts.ets` 的中文、英文、数字、电话和符号布局中删除 `hideKey()`。
2. 新建独立的 `KeyboardStatusBar` 组件，状态栏采用“左侧状态/候选区域 + 右侧固定操作区”的结构。
3. 在状态栏右侧增加收起图标，点击后派发已有的 `HIDE_KEYBOARD_ACTION`，继续复用现有键盘隐藏、组合清理和用户模型保存流程。
4. 候选区使用剩余宽度并允许滚动；右侧操作区固定宽度，不参与候选横向滚动。
5. 给图标增加“收起键盘”的无障碍说明、足够的点击热区、按下态和触觉反馈。
6. 删除主键盘隐藏键后，重新分配空格行各键权重，优先扩大空格键和回车键。
7. 更新所有依赖底部行键序的自动测试；补充“候选很多时仍能收起键盘”和“连续点击只执行一次隐藏流程”的回归测试。

#### 涉及模块

- `entry/src/main/ets/presentation/keyboard/layouts/Stage10KeyboardLayouts.ets`
- `entry/src/main/ets/presentation/keyboard/KeyboardRootStage3.ets`
- 建议新增 `entry/src/main/ets/presentation/keyboard/components/KeyboardStatusBar.ets`
- `entry/src/main/ets/application/KeyboardController.ets`
- `entry/src/main/ets/application/InputSessionController.ets`
- `entry/src/test/InputPanelLifecycle.test.ets`

#### 验收标准

- 所有键盘模式的主键区都没有隐藏键；
- 状态栏右侧始终可以收起键盘；
- 收起前存在未提交输入时，行为与现有隐藏流程一致，不重复提交、不遗留候选；
- 收起按钮点击热区不小于普通功能键的可用热区，无障碍朗读正确。

### 3.3 Shift 键放在 Z 左侧，并根据中文输入状态切换功能

#### 客户反馈

Shift 键通常使用“↑”表示，通常位于 Z 键左侧。中文键盘有输入时作为分词键，无输入时作为大写锁定键。

#### 修改目标

Shift 键位置固定，但中文模式下根据组合状态显示并执行不同功能：

| 中文组合状态 | 图标/文案 | 点击行为 |
| --- | --- | --- |
| `rawInput` 为空 | `↑`，锁定时显示激活态 | 开启或关闭大写直输锁定 |
| `rawInput` 非空 | 分词符号，建议显示 `'` 或“分” | 在当前位置加入显式分词边界并重新解析候选 |

“大写直输锁定”建议定义为：保持中文键盘界面，但后续字母直接向编辑框提交 A～Z，不进入中文引擎；再次点击 Shift 后回到中文编码输入。切换编辑框、切换键盘模式或会话结束时自动退出锁定，避免状态泄漏。

#### 具体修改步骤

1. 在中文布局第三行的 Z 左侧增加一个动态功能键，位置和宽度与英文 Shift 一致。
2. 在会话状态中增加中文 Shift 状态，例如 `chineseCapsLock`；该状态不能复用英文单次大写状态，以免两种状态互相干扰。
3. 布局渲染时读取 `rawInput.length`：为空时使用 Shift 图标及激活态；非空时切换为分词图标和无障碍文案“插入分词”。
4. 扩展键盘动作，明确区分 `TOGGLE_CHINESE_CAPS_LOCK` 和 `INSERT_SEGMENT_BOUNDARY`，不要让 UI 根据点击后时机自行猜测动作。
5. 在 `InputSessionController.insertLetter` 中增加路由：中文大写锁定开启时，将字母转换为大写并直接提交，不调用中文引擎。
6. 为分词动作定义独立引擎语义：仅在非空组合中插入边界、重新解析并返回新的 `CompositionResult`，不能把字面量 `'` 直接上屏。
7. 如果现有 `processKey` 只允许小写字母，则在 ArkTS、C++、Rust 四层增加明确的 `insertSegmentBoundary` 能力，并同步更新接口契约；不要把特殊字符偷偷塞入普通字母接口。
8. 分词后同步更新编辑框预编辑文本、状态栏输入码和候选列表；退格时应先删除边界或最后一个编码，结果必须可预测。
9. 在模式切换、键盘隐藏、焦点改变、会话结束和异常 reset 时清空中文大写锁定与分词临时状态。
10. 增加状态机测试，覆盖空输入、非空输入、连续点击、分词后退格、分词后选词、锁定后直输大写及会话切换。

#### 涉及模块

- `entry/src/main/ets/domain/keyboard/KeyboardAction.ets`
- `entry/src/main/ets/presentation/keyboard/layouts/Stage10KeyboardLayouts.ets`
- `entry/src/main/ets/presentation/keyboard/KeyboardRootStage3.ets`
- `entry/src/main/ets/state/InputSessionStore.ets`
- `entry/src/main/ets/application/KeyboardController.ets`
- `entry/src/main/ets/application/InputSessionController.ets`
- `entry/src/main/ets/application/EngineCoordinator.ets`
- `entry/src/main/ets/infrastructure/native/NativeEngineTypes.ets`
- Rust 解析器、C ABI 和 C++ Node-API 桥接层

#### 验收标准

- Shift/分词键始终位于 Z 左侧；
- 中文无输入时点击可开启和关闭大写直输，输入结果为 A～Z；
- 中文有输入时按键立即变为分词功能，不改变为大写锁定；
- 分词不会把 `'` 当作普通字符提交到编辑框；
- 分词后的候选、退格和选词结果确定且无崩溃；
- 切换编辑框后不保留上一个编辑框的大写锁定或分词状态。

### 3.4 中文键盘字母使用大写显示

#### 客户反馈

中文键盘通常使用大写字母。

#### 当前实现（2026-07-21）

已完成。正式中文布局在普通组合、大写直输锁定和非空分词状态下均固定使用大写显示标签；`createLetterInputAction(letter)`、`rawInput` 和 Rust 查询键仍保持小写。预览气泡和无障碍文案与键帽标签一致，英文 Shift/Caps Lock 状态机未改变。手机与 Pad 模拟器均已通过定向验收，真实设备待补。

#### 具体修改步骤

1. 将中文布局的字母键标签统一改为 `A～Z`。
2. 保持 `createLetterInputAction(letter)` 传递原有字母值，并由输入控制器继续归一化为小写编码。
3. 中文大写直输锁定开启时，通过 Shift 激活态表达“直接输入大写”，不能仅依靠键帽大小区分，因为中文普通状态下键帽也为大写。
4. 检查按键预览气泡、无障碍文案和键帽三处显示一致。
5. 增加测试：中文键帽为大写，但送入引擎的 `rawInput` 仍为小写；英文键盘仍按英文 Shift 状态显示大小写。

#### 验收标准

- 中文键盘所有字母键帽和预览气泡均显示大写；
- 中文编码不因视觉大写而改变，候选结果与修改前一致；
- 英文小写、单次大写和大写锁定行为不受影响。

### 3.5 Pad 和手机的输入法切换键、底部辅助区

#### 实施状态

已完成（模拟器验收）。Pad 文本键盘在空格行加入切换键，短按成功切换到下一已启用输入法，长按打开系统选择器；手机在高度预算允许时显示独立底部辅助行，左切换、右收起，中间留白，不压缩主键区。能力适配、直接切换失败后的系统选择器回退、并发/连点保护、会话失效保护和短长按互斥均已实现。Phone 1320×2856 与 Pad 2880×1920 模拟器通过自动验收；物理真机、三键导航和外接键盘仍待补测。

#### 客户反馈

Pad 键盘空格行考虑加入切换输入法键，长按弹出选项；手机键盘可考虑在空格下面增加架空行，左右放输入法切换键和键盘隐藏键。

#### 修改目标

- Pad：在空格行左侧增加系统输入法切换键。短按切换到下一个输入法，长按打开输入法列表。
- 手机：优先保留标准四行主键盘；在设备有足够底部安全区时增加轻量辅助行，左侧为输入法切换，右侧为收起键盘。
- 如果系统版本不允许第三方输入法直接切换，短按和长按都降级为打开系统输入法选择面板，不能显示一个点击无反应的按钮。

#### 具体修改步骤

1. 在 `ScreenMetrics` 或独立设备形态策略中加入 `phone/tablet` 判定。建议按可用短边 `vp` 和窗口形态判断，不依赖具体机型名称。
2. 先做 HarmonyOS 目标 API 的能力验证：确认输入法 Extension 是否可调用“切换下一个输入法”和“显示输入法选择器”。将验证结果、最低 API 和不支持时的回退写入技术记录。
3. 在系统集成层定义 `InputMethodSwitcher` 接口，UI 只调用该接口，不直接依赖具体系统 API。
4. 增加 `SWITCH_INPUT_METHOD` 和 `SHOW_INPUT_METHOD_PICKER` 动作；Pad 空格行切换键绑定短按，长按绑定选择器。
5. 手机辅助行只在可用高度充足、不会挤压字母键且底部安全区允许时显示；空间不足时隐藏辅助行，但顶部状态栏的收起键仍然可用。
6. 长按弹窗应使用系统输入法选择器；如果必须使用应用内弹层，只能展示系统允许枚举且可实际切换的项目。
7. 处理长按与点击互斥：长按成功后不能再触发一次短按。
8. 增加 Pad、手机、横屏、手势导航、三键导航和外接键盘场景测试。

#### 验收标准

- Pad 可以从空格行发起输入法切换，长按能看到可选输入法；
- 手机底部辅助行不会压缩或遮挡主键区；
- 系统不支持直接切换时有明确且可用的系统选择器回退；
- 长按不会额外触发短按；
- 状态栏收起键与手机辅助行收起键调用同一隐藏流程。

### 3.6 空闲状态栏不留白

#### 客户反馈

状态栏无内容，是否尚未设计？

#### 当前问题

当前文本键盘会一直预留候选栏高度，以避免首次输入时键盘跳动；但空闲状态下 `CandidateBar` 没有渲染任何内容，所以用户看到一条空白区域，容易误认为功能未完成。

#### 修改目标

将顶部区域明确设计为“状态栏/候选栏共用区域”：

- 无输入：显示当前方案、中文/英文状态、设置入口或键盘工具，以及固定的收起键；
- 有输入无候选：显示按两码切分的输入码和必要的错误提示；
- 有候选：显示候选词，工具按钮收纳或只保留固定操作；
- 错误状态：显示简短可理解的错误提示，不能直接展示内部错误码或调试信息。

#### 具体修改步骤

1. 将现在的 `CandidateBarState.IDLE` 从“空组件”改为 `StatusToolbarView`。
2. 左侧显示当前输入模式和方案名，例如“中 · 双羽”；方案名必须来自设置状态，不能写死。
3. 中间预留工具入口。首版建议只放设置/更多，避免一次加入过多未完成按钮。
4. 右侧固定放置收起键；候选态也保持该固定区域。
5. 输入后状态栏切换为输入码/候选内容，栏高保持不变，避免键盘整体跳动。
6. 为四种状态建立明确优先级：错误 > 候选 > 预编辑 > 空闲工具栏。
7. 补充深色模式、浅色模式、大字号、长方案名和窄屏的截断规则。
8. 增加 UI 状态测试，确保空闲态不是纯空白。

#### 涉及模块

- `entry/src/main/ets/presentation/candidate/CandidateBar.ets`
- `entry/src/main/ets/presentation/keyboard/KeyboardRootStage3.ets`
- 建议新增 `KeyboardStatusBar.ets`
- `entry/src/main/ets/state/SettingsStore.ets`
- `entry/src/main/ets/presentation/keyboard/design/KeyboardPalette.ets`

#### 验收标准

- 键盘刚打开时顶部区域有明确内容，不再是一整条空白；
- 空闲、输入、候选和错误状态切换时键盘高度不跳动；
- 收起键始终可见；
- 方案名、模式和主题状态显示正确。

### 3.7 非拼音方案支持按键选择第二候选

#### 客户反馈

如果是非拼音方案，需要考虑设定某个键，在有候选时作为第二候选上屏键使用，不必直接点击候选窗。

#### 修改目标

为码表类、形码类等非拼音方案提供“第二候选快捷键”。默认建议使用 `;`，但必须做成方案配置；最终默认键由双羽正式规则确认。

行为定义：

- 当前为适用方案且至少有两个候选：按快捷键提交索引 1 的候选；
- 只有一个候选：不越界，不误提交；可按方案规则输入原符号或不处理；
- 没有候选：按原键盘规则输入该符号；
- 拼音/双拼方案：保持原有标点行为，除非产品规则明确启用。

#### 具体修改步骤

1. 给方案元数据增加 `secondCandidateKey` 和 `supportsCandidateHotkeys`，不要在控制器中写死某一个方案 ID。
2. 在键盘动作中增加 `SELECT_CANDIDATE_BY_INDEX`，携带从 0 开始的候选索引；第二候选固定传 1。
3. 屏幕键盘点击该快捷键和外接键盘按下同一物理键时，都走同一动作路由。
4. 在 `KeyboardController` 中先读取当前方案、键盘模式和候选数量，再决定“选第二候选”或“输入原符号”。
5. 复用现有 `commitCandidate(1)`，确保提交、用户学习、预编辑清理和剩余组合处理与点击候选完全一致。
6. 对候选不足、提交进行中、密码框、候选栏禁用和会话已失效等情况保持现有保护。
7. 在设置页提供快捷键说明；若允许用户更改，限制在方案声明的可用键集合内，避免与分词键、引导键或普通编码键冲突。
8. 增加屏幕键盘与外接键盘测试，验证第二候选确实通过索引 1 提交，而不是复制候选显示文本直接插入。

#### 验收标准

- 有两个及以上候选时，快捷键可以稳定提交第二候选；
- 候选不足时不会越界、崩溃或误上屏；
- 提交第二候选会进入现有用户学习流程；
- 非适用方案下该键保持原有输入行为；
- 屏幕键盘与外接键盘结果一致。

### 3.8 浮动候选窗，候选跟随光标

#### 客户反馈

考虑增加浮动候选窗，让候选跟随光标。Pad 大屏和使用外接键盘时更容易看到候选。

#### 修改目标

在 Pad 或外接物理键盘场景提供可选的浮动候选窗，锚定当前编辑光标附近；手机软键盘默认继续使用顶部候选栏。浮动窗不可遮挡正在输入的文本，并应在跨应用、旋转、分屏和窗口移动时更新位置。

#### 实施前置条件

该功能不能只按普通 ArkUI 弹窗实现。必须先确认输入法 Extension 能否可靠获取光标矩形、创建跨应用可见的输入法附属窗口，以及目标 HarmonyOS 版本的权限和生命周期限制。

#### 具体修改步骤

1. 建立独立能力验证任务，验证以下三项：能否获取光标/插入点矩形；能否创建受输入法生命周期管理的浮动窗；能否在目标应用、Pad 分屏和外接键盘下稳定显示。
2. 若只能获取光标索引而不能获取屏幕坐标，则本期不承诺“跟随光标”，改为 Pad 屏幕固定侧边候选条，并在文档中明确限制。
3. 能力验证通过后，增加 `CandidatePresentationMode`：`bar`、`floating`、`auto`。
4. `auto` 模式建议在 Pad 或检测到外接物理键盘且软键盘未占主要屏幕空间时启用浮动候选。
5. 新建 `FloatingCandidateWindowController`，负责创建、更新、隐藏和销毁窗口；候选数据仍只来自 `InputSessionStore`，不能建立第二套候选状态。
6. 根据光标矩形、窗口边界、安全区和候选窗尺寸计算位置：优先放光标下方，空间不足时放上方，左右越界时回缩。
7. 输入、移动光标、旋转、分屏、窗口移动、焦点切换和会话结束时更新或关闭浮动窗。
8. 浮动窗候选点击继续调用 `commitCandidate(index)`；隐藏或销毁浮动窗不能清除仍有效的输入组合。
9. 设置页增加“候选窗位置：自动/键盘顶部/跟随光标”，在系统不支持时禁用“跟随光标”并给出说明。
10. 做跨应用兼容测试，至少覆盖系统文本框、浏览器、文档编辑、聊天输入框、Pad 分屏和外接键盘。

#### 验收标准

- 支持的设备上，浮动窗稳定跟随光标且不超出窗口边界；
- 光标下方空间不足时能自动翻到上方；
- 焦点切换和输入会话结束后浮动窗立即消失；
- 浮动候选与顶部候选使用同一份数据、同一提交路径；
- 不支持的设备有清晰回退，不出现悬空窗口或权限错误。

## 4. 输入方面

### 4.1 嵌入式输入码增加“未上屏”标识，优先使用下划线

#### 客户反馈

中文键盘输入码采用嵌入方式时，可增加下划线，表示这些字母仍处于未上屏状态。

#### 当前基础

项目已经通过 HarmonyOS 文本预编辑接口把 `rawInput` 放入编辑框，并在候选确认时结束预编辑。该接口语义上已经区分“组合文本”和“正式提交文本”，但不同宿主应用是否明显显示下划线需要真机验证。

#### 修改目标

用户在编辑框中必须能明显分辨当前输入码尚未上屏。优先使用系统预编辑样式；宿主应用不展示系统下划线时，在顶部状态栏同步显示带下划线的输入码作为回退提示。

#### 具体修改步骤

1. 保持 `setPreviewText`/`finishTextPreview` 为唯一的编辑框组合文本管理方式，不把未确认编码改为普通 `insertText`。
2. 在系统文本框、浏览器、文档、聊天输入框等目标应用上验证预编辑文本是否自带下划线或组合样式。
3. 检查目标 API 是否允许输入法指定预编辑文本样式；如果系统开放合法样式接口，则统一设置单下划线和主题适配颜色。
4. 如果系统不允许第三方输入法控制宿主编辑框样式，不使用删除再插入等方式伪造下划线，以免破坏光标和撤销栈。
5. 在 `CandidateBar` 的预编辑区域对输入码增加下划线装饰，作为所有应用都一致的视觉回退。
6. 候选上屏、空格确认、回车确认、删除至空、切换编辑框、隐藏键盘和异常 reset 时必须结束或清理预编辑范围。
7. 增加“预编辑支持”和“不支持预编辑”两条测试路径，确保回退只影响展示，不改变候选与提交结果。

#### 验收标准

- 支持系统预编辑样式的编辑框中，编码有明确未上屏视觉；
- 不支持样式的应用中，状态栏仍能看到带下划线的当前编码；
- 候选提交后下划线和预编辑状态立即消失；
- 切换焦点或隐藏键盘后不残留旧编码。

### 4.2 候选词后不再显示全拼码

#### 客户反馈

输入时不要在候选后面显示全拼码。对双拼用户展示全拼码会造成干扰，就像全拼输入时在候选后显示双拼码一样。

#### 当前问题

当前 `CandidateBar` 会同时渲染 `candidate.text` 和 `candidate.reading`，因此每个候选后都带有读音/全拼信息。

#### 修改目标

普通候选栏只显示候选文字，不显示 `candidate.reading`。读音字段仍可保留在引擎协议中用于查询、排序、调试或无障碍扩展，但不能默认显示在候选词后。

#### 具体修改步骤

1. 从 `CandidateBar.CandidatesView` 中移除候选后的 `Text(candidate.reading)`。
2. 重新调整候选项的左右内边距和候选间距，避免移除读音后显得过于松散。
3. 候选项只用 `candidate.text` 计算可见宽度，长词按现有规则截断或滚动。
4. Release 界面不显示 `reading`；如内部调试页仍需查看，放到 internalDebug 页面或显式调试开关下。
5. 更新 UI 测试，断言候选只显示文字；引擎和协议测试仍保留 `reading` 字段，避免误删核心数据。

#### 验收标准

- 所有普通候选后均不显示全拼或其他编码；
- 候选点击、排序和学习行为不变；
- 候选栏空间利用率提高，同屏可见候选数量不低于修改前；
- Release 包无调试读音泄漏。

### 4.3 输入码按两码切分显示

> 实现状态（2026-07-29）：已完成。Rust `CompositionResult` 输出
> `displaySegments`，UI 只连接引擎段并保留下划线“未上屏”样式。客户示例
> `vegewtyijkjj` 已纳入自动测试并显示为 `ve'ge'wt'yi'jk'jj`。

#### 客户反馈

输入码采用两码切分方式：有完整声韵组合时显示对应的两码段，没有完整声韵组合时显示简拼组合。客户示例为：`ve'ge'wt'yi'jk'jj`，对应输入“这个问题已经解决”。

#### 修改目标

输入码展示应体现双拼/双羽的“两码一段”，而不是在候选后显示全拼：

- 每两个有效编码组成一段，段与段之间显示 `'`；
- 末尾只有一个编码时保留为未完成段；
- 无法按完整声韵解析时，按方案规则显示为简拼组合，不擅自展开成全拼；
- 显示字符串只用于用户界面，候选查询仍使用引擎的结构化解析结果。

当前实现只呈现原始编码的两码段，不推断客户示例对应的汉字语义，也不使用全拼
`preeditText` 代替输入码，因此不依赖双羽映射表来生成展示字符串。

#### 具体修改步骤

1. `engine-protocol::CompositionResult` 保留 `rawInput`、`parsedSyllables`、
   `pendingCode` 和 `segmentBoundaries`，新增 `displaySegments: string[]`。
2. Rust 在每次生成组合结果时同步生成展示段；UI 不自行解析声母、韵母或简拼。
3. C++ Node-API、ArkTS Native 网关和 `InputSessionStore` 原样转发展示段。
4. `CompositionPreeditView` 只使用 `'` 连接引擎段；分隔符不写回原始编码。
5. `CandidateBar`、Pad 固定侧栏和保留浮动组件共用同一展示函数；编辑框系统预览链路保持不变。
6. 候选提交、部分提交、退格和显式分词后的新 `CompositionResult` 会重新生成展示段。
7. `Stage43.test.ets` 覆盖完整双码、奇数尾码、显式分词、引导字符、空输入、旧协议安全回退及客户示例。
8. 稳定候选基线已按新增 JSON 字段更新；候选内容、顺序和分页未变化。

#### 涉及模块

- Rust 双拼/双羽解析器与 `engine-protocol`
- C ABI、C++ Node-API 结果转换
- `entry/src/main/ets/infrastructure/native/NativeEngineTypes.ets`
- `entry/src/main/ets/state/InputSessionStore.ets`
- `entry/src/main/ets/presentation/candidate/CandidateBar.ets`
- `docs/API_CONTRACT.md`

#### 验收标准

- 输入码稳定按两码分段显示，分隔符不被上屏；
- 奇数长度输入能保留最后一个未完成码；
- 无完整声韵时按正式规则显示简拼组合；
- 删除、分词和部分上屏后的分段立即正确刷新；
- UI 不再用候选的全拼 `reading` 代替输入码展示。

### 4.4 简拼结果支持用户自定义，增加确定性

#### 客户反馈

简拼如果能自定义结果会更好，可以增加输入结果的确定性。

#### 当前基础

项目已有独立用户词库覆盖层，支持“词条 + 编码”，并支持 `#固` 固顶、`#N` 指定候选位置和 `#删` 删除特定候选。现阶段缺少面向普通用户的管理页面和简拼专用编辑流程。

#### 修改目标

允许用户为一个简拼编码指定词语，并选择固顶或候选位置。例如用户为某个简拼设置固定短语后，每次输入该简拼都得到稳定结果，不被自动学习或基础词频随机改变。

#### 具体修改步骤

1. 复用现有用户词库格式和硬排序能力，不另建一套与候选排序冲突的“简拼收藏”数据库。
2. 在设置页增加“自定义简拼”入口，列表项至少包含：简拼编码、目标词语、排序方式、启用状态。
3. 新增和编辑时执行校验：编码只能使用当前方案允许的字符和长度；词语不能为空；禁止与分词键、第二候选键或引导前缀形成未定义冲突。
4. 默认保存为固顶规则 `词条<TAB>编码#固`；高级选项允许使用 `#N` 指定第 N 候选。
5. 同一“编码 + 词条”再次保存时按现有“后规则覆盖前规则”处理；删除时只删除对应用户规则，不修改系统词库和自动学习数据。
6. 保存继续使用现有原子写入、备份和恢复机制，避免应用中断导致整个用户词库损坏。
7. 为运行时增加安全的用户词库 reload 能力，或在保存后受控重建引擎句柄；重载前先清空当前组合，不能让半旧半新的候选快照进入同一次查询。
8. 候选排序严格保持“基础排序和自动学习先完成，用户 `#固/#N` 最后硬覆盖”，保证自定义结果不会被学习分挤走。
9. 提供导出、导入前应先完成格式校验和冲突预览；首版可只做本机新增、编辑和删除。
10. 增加测试：新增后立即生效、重启后保留、固顶不受学习影响、指定位置正确、重复规则覆盖、删除恢复系统候选、损坏文件从备份恢复。

#### 涉及模块

- `docs/USER_LEXICON_FORMAT.md`
- Rust `user-lexicon`、`ime-engine` 和运行时重载接口
- `entry/src/main/ets/presentation/settings/SettingsPage.ets`
- `entry/src/main/ets/application/SettingsController.ets`
- 用户词库路径、命令仓库和 Native 网关

#### 验收标准

- 用户可以新增、编辑和删除自定义简拼结果；
- 固顶结果每次都在首位，指定位置结果稳定出现在目标位置；
- 自动学习不会覆盖用户的硬排序；
- 修改在安全重载后立即生效，重启应用后仍存在；
- 用户规则损坏时可回退，不影响系统基础词库输入。

## 5. 推荐实施顺序

### 第一阶段：先解决键盘观感和高频操作（P0）

1. 统一正式 QWERTY 布局；
2. 中文键帽改为大写；
3. Shift 移到 Z 左侧，先完成无输入大写直输和 UI 状态；
4. 隐藏键移到顶部状态栏；
5. 设计空闲状态栏内容；
6. 移除候选后的全拼码；
7. 完成手机、Pad、横竖屏和深浅色真机验收。

这一阶段完成后即可交付下一次客户体验，因为改动最直观，并且不会等待浮动窗等高风险系统能力。

### 第二阶段：完善方案输入效率（P1）

1. 完成中文 Shift 的显式分词引擎能力；
2. 完成两码分段的结构化协议和显示；
3. 加入非拼音方案第二候选快捷键；
4. 完成 Pad/手机输入法切换入口；
5. 完成自定义简拼管理和安全重载；
6. 验证并加强预编辑下划线提示。

### 第三阶段：大屏和外接键盘增强（P2）

1. 验证光标矩形和输入法附属窗口能力；
2. 能力通过后实现浮动候选窗；
3. 完成 Pad 分屏、外接键盘和跨应用兼容性验收；
4. 能力不通过时交付固定侧边候选条作为降级方案。

## 6. 必须向产品或客户确认的事项

以下事项会直接改变输入结果，开发前必须书面确认：

1. “双羽方案”的正式编码映射表和版本号；
2. 客户示例 `ve'ge'wt'yi'jk'jj` 的完整预期分段及对应文字；
3. 中文有输入时“分词键”的准确规则：仅插入边界，还是提交前一段后继续输入；
4. 中文无输入时“大写锁定”的预期：中文界面内大写直输，还是切到英文大写键盘；
5. 非拼音方案默认的第二候选键，以及候选不足时该键应输入符号还是无动作；
6. 哪些方案属于“非拼音方案”，哪些方案启用第二候选快捷键；
7. 手机底部辅助行是始终显示，还是仅在存在足够安全区时显示；
8. 浮动候选窗是否默认开启，以及不支持跟随光标时是否接受固定侧边候选条；
9. 自定义简拼首版是否只做本机管理，是否需要导入、导出和分享。

## 7. 整体验收清单

- [ ] 中文、英文键盘结构符合通用 QWERTY 习惯；
- [ ] Shift/分词键位于 Z 左侧，删除键位于 M 右侧；
- [x] 中文字母键帽统一显示大写；
- [ ] 隐藏键已从主键盘移至状态栏；
- [ ] 空闲状态栏有模式、方案或工具内容，不再留白；
- [x] Pad 和手机的输入法切换入口按设备策略正确显示；
- [ ] 非拼音方案可以通过快捷键提交第二候选；
- [ ] 编辑框或状态栏能明确表示输入码尚未上屏；
- [ ] 候选词后不显示全拼码；
- [x] 输入码按正式方案规则进行两码切分；
- [ ] 简拼自定义结果可固顶或指定位置，并能持久化；
- [ ] 浮动候选窗完成能力验证，并有明确实现或降级结论；
- [ ] 手机、Pad、横竖屏、深浅色、外接键盘和常见宿主应用均完成回归；
- [ ] 所有组合状态在隐藏键盘、切换焦点和会话结束后正确清理。
