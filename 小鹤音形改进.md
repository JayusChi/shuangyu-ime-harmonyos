# 小鹤音形改进

更新时间：2026-07-27

## 1. 文档目的

本文用于规划 HarmonyOS 输入法中 `xiaohe-yinxing` 的分阶段改进。当前正式音形包、八分类、用户规则、分页、四码提交、第五键顶屏、空码切分、方案切换和资源安装已经可用，但整体仍偏向“可靠的静态码表运行时”，与成熟的小鹤音形产品在渐进候选、学习调频、查形辅助、扩展词库、连续输入和移动端体验方面存在差距。

本文按“先修核心输入语义，再补高频体验，最后扩展功能和完成发布验收”的顺序推进。每个阶段应独立开发、测试、验收和记录，不应一次性把所有延期数据或高风险动作接入正式包。

状态标记：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成并通过阶段门禁
- `[!]` 阻断或需要产品确认

## 2. 当前基线

### 2.1 已完成能力

- 正式方案 ID：`xiaohe-yinxing`。
- 正式 bundle：`xiaohe-yinxing-production.hsyx`。
- 正式数据：73,263 条普通记录、36 条内置固顶规则。
- 八个正式分类默认启用，`core` 必选。
- 精确查询、无精确时的前缀回退、跨分类稳定去重。
- 用户词库 `#删/#固/#N` 覆盖。
- 完整候选合并后分页。
- 四码唯一候选自动提交、第五键顶屏。
- 空码清屏、正向和反向空码切分。
- 正式资源安装、大小和 SHA-256 校验、损坏回退。
- 设置页可在小鹤双拼和小鹤音形之间切换。

### 2.2 当前主要问题

当前查询合同是“存在精确编码时，只返回精确项；没有精确项时，才返回更长编码的前缀项”。这适合通用码表的精确优先模式，却不完全符合小鹤音形“前两码检索同音字、后两码逐步按形筛选”的输入过程。

正式八类数据的实际分布：

| 输入长度 | 编码数 | 只有 1～2 个精确候选的比例 |
| --- | ---: | ---: |
| 1 码 | 26 | 100% |
| 2 码 | 657 | 100% |
| 3 码 | 5,494 | 100% |
| 4 码 | 62,716 | 约 99.6% |

如果按完整前缀统计，二码平均约有 101.8 个候选，中位数为 81 个，91.9% 的二码拥有超过 9 个后续候选。当前精确排他行为把这些候选隐藏了。例如：

| 编码 | 当前精确候选数 | 完整前缀候选数 |
| --- | ---: | ---: |
| `aa` | 1 | 41 |
| `ai` | 1 | 115 |
| `an` | 1 | 165 |
| `ni` | 1 | 94 |
| `hc` | 1 | 209 |
| `ui` | 2 | 343 |
| `vi` | 2 | 354 |

因此，“每次只出现一两个候选”主要是查询语义问题，不是正式 bundle 损坏，也不是候选栏限制为两个。

### 2.3 已知延期范围

首版转换合同仍延期以下能力：

- 拼字。
- 完整符号组。
- 全部直通。
- 简繁转换。
- 暗词库。
- 纠错。
- 英文补全。
- 快符。
- 日期时间动作。
- 成对符号动作。
- 光标动作。

不得为了追求功能数量而直接执行原始配置中的脚本、外部程序、网络、URL、FTP、WebDAV、动态路径或平台专用命令。

## 3. 总体实施顺序

| 阶段 | 优先级 | 目标 | 前置依赖 |
| --- | --- | --- | --- |
| 阶段 0 | P0 | 冻结改进合同和回归基线 | 无 |
| 阶段 1 | P0 | 修复一至三码渐进候选 | 阶段 0 |
| 阶段 2 | P0/P1 | 增加音形编码提示、候选展开和查形入口 | 阶段 1 |
| 阶段 3 | P1 | 增加音形专用本地调频与用户词库编辑 | 阶段 1 |
| 阶段 4 | P1/P2 | 安全接入拼字、符号、简繁和静态引导数据 | 阶段 1、阶段 2 |
| 阶段 5 | P2 | 增强联想、补全、智能标点和连续输入 | 阶段 3 |
| 阶段 6 | P1 | 优化 bundle 加载、内存和首键延迟 | 阶段 1，可与 2～5 并行 |
| 阶段 7 | P0 | 完成真机、稳定性和发布验收 | 阶段 1～6 中纳入本次发布的部分 |

## 4. 阶段 0：冻结改进合同和基线

### 目标

在修改查询语义前，记录当前正式包、候选快照、性能和设备行为，防止后续无法判断变化是预期改进还是回归。

### 任务

- [ ] 冻结当前正式 bundle 的大小、SHA-256、manifest 和八分类记录数。
- [ ] 保存 `aa/ai/an/ni/hc/ui/vi/wo/xm/xq` 的旧候选快照。
- [ ] 保存四码唯一、多重码、无结果、用户删除、固顶、位置规则的旧快照。
- [ ] 记录正式音形的加载 P50/P95、逐键查询 P50/P95、峰值内存和首键延迟。
- [ ] 新建或更新 ADR，明确小鹤音形采用“渐进候选”，通用 fixture 是否继续保留“精确排他”。
- [ ] 明确候选混合规则只影响 `xiaohe-yinxing`，不得改变 `xiaohe` 双拼行为。
- [ ] 明确 Release 第一阶段仍保持离线，不新增网络权限。

### 建议决策

不要直接修改通用 `query_exact_or_prefix` 的既有语义。优先增加“小鹤音形专用查询策略”或显式查询模式，避免破坏 fixture、用户词库工具和其他码表测试。

### 验收标准

- [ ] 基线证据可重复生成。
- [ ] 正式包源文件没有被修改。
- [ ] ADR 明确一至四码行为、排序、去重、分页、用户规则和自动上屏条件。
- [ ] 所有既有测试在功能修改前保持通过。

## 5. 阶段 1：修复渐进候选核心语义

这是最高优先级阶段，也是解决“候选只有一两个”的关键。

### 5.1 目标行为

#### 一码

- 精确一简候选优先。
- 再使用以该码开头的二至四码候选补充。
- 按候选文字稳定去重。

#### 二码

- 精确二简候选优先。
- 同时展示该双拼音节下的三码、四码候选。
- 用户能够在输入前两码后看到完整同音候选列表。

#### 三码

- 精确三码候选优先。
- 再展示匹配前三码的四码候选。
- 第三码应明显缩小二码候选范围。

#### 四码

- 只返回完整四码精确候选。
- 不混入其他更长路径。
- 唯一精确候选且不存在有效后续码时，继续自动上屏。

### 5.2 排序规则

建议排序顺序：

1. 用户固顶。
2. 当前输入的精确简码候选。
3. 系统更长编码候选。
4. 用户指定的全局位置。
5. 分类顺序。
6. 分类内 `source_order`。

必须保持：

- 同文字跨分类稳定去重。
- 完整列表合并后再分页。
- 用户精确删除后不错误重新打开被删除的同一路径。
- `core` 必选和分类开关继续生效。
- 同一次操作使用同一个分类和用户词库不可变快照。

### 5.3 实现任务

- [ ] 为查询运行时增加显式策略，例如 `ExactOnly`、`ExactOrPrefixFallback`、`ProgressiveXiaoheYinxing`。
- [ ] 为一至三码实现“精确项在前＋前缀项补充”。
- [ ] 四码继续使用精确查询。
- [ ] 将精确候选和前缀候选在分页前合并、去重。
- [ ] 保持最大候选数量限制，避免一次查询返回无界结果。
- [ ] 检查 `has_valid_continuation` 是否仍使用完整、过滤后的候选集合。
- [ ] 检查四码唯一自动提交不受混合候选列表误判。
- [ ] 检查第五键顶屏只提交上一段的正确首选。
- [ ] 检查空码切分仍以完整编码语义执行，不把二码前缀候选误当成可提交完整段。
- [ ] 更新 Rust、FFI、ArkTS 和设备端回归。

### 5.4 必测用例

- [ ] `ni` 第一页包含“你”以及其他 `ni*` 同音候选。
- [ ] `aa` 第一页包含“阿”以及其他 `aa*` 候选。
- [ ] `ui` 的两个精确简码保持在前，同时补充 `ui*` 候选。
- [ ] 输入第三码后候选集合是二码集合的合理子集。
- [ ] 输入完整四码后只保留完整码候选。
- [ ] 四码唯一候选继续只提交一次。
- [ ] 多重四码不自动提交，可正常选择和翻页。
- [ ] 用户 `#固/#删/#N` 在混合列表中行为稳定。
- [ ] 关闭可选分类后，候选、唯一性和自动提交使用同一分类快照。
- [ ] 切回 `xiaohe` 后“你好/输入法”等双拼回归完全不变。

### 5.5 阶段验收标准

- [ ] 二码不再普遍只有一两个候选。
- [ ] `aa/ai/an/ni/hc/ui/vi` 均能展示完整的渐进候选第一页。
- [ ] 一至三码候选顺序确定、重复构建结果一致。
- [ ] 四码提交、第五键、空码切分、用户规则和分类回归全部通过。
- [ ] Rust workspace、Clippy、ArkTS、双 ABI 和 Release 正负门禁通过。

## 6. 阶段 2：编码提示、候选展开和查形

### 目标

让新用户能够理解第三、第四色形码，并让熟练用户可以关闭提示保持简洁。

### 任务

- [ ] 新增“音形编码提示”设置，仅在 `xiaohe-yinxing` 下生效。
- [ ] 提示开启时，在候选中显示完整编码或剩余形码。
- [ ] 提示关闭时，保持当前纯候选文字样式。
- [ ] 候选展开面板展示超过首屏的完整列表。
- [ ] 展开面板支持分页或虚拟列表，不能一次创建数百个 ArkUI 节点。
- [ ] 长按候选提供“查看编码/查形”入口。
- [ ] 增加光标前汉字查形入口。
- [ ] 增加不知道形码时的万能键或显式“展开同音字”操作。
- [ ] 为形码提示、查形和万能键增加无障碍文案。
- [ ] 增加简短的音形入门页：双拼键位、双形规则、简码、四码和顶屏说明。

### 验收标准

- [ ] 新手可以只输入二码、查看编码提示并完成目标字选择。
- [ ] 熟练用户可以关闭所有编码提示。
- [ ] 展开 300 个以上候选时无明显卡顿或内存峰值异常。
- [ ] 查形只读取本地数据，不上传用户文本。
- [ ] 双拼候选栏样式不受音形专用设置影响。

## 7. 阶段 3：本地调频和用户词库编辑

### 目标

在不破坏码表稳定性的前提下，提供可选择、可撤销、完全本地的个性化排序。

### 产品原则

- 默认排序必须稳定。
- 动态调频应提供总开关。
- 学习只在同完整编码或明确的同级候选范围内调序。
- 不应因为上下文学习改变汉字的实际编码。
- 用户必须能够查看、导出、清除和恢复个性化数据。

### 任务

- [ ] 为音形后端增加独立的用户选择计数模型。
- [ ] 设计稳定排序公式，用户频率只能作为受控排序层。
- [ ] 长按候选支持“固顶、删除、移到第 N 位、恢复默认”。
- [ ] 增加用户词库管理页面。
- [ ] 支持导入前校验、原子替换和失败回滚。
- [ ] 支持导出、备份、恢复和清空。
- [ ] 保留内置 36 条规则，外部规则按既有覆盖合同叠加。
- [ ] 增加损坏主文件、有效备份恢复和跨进程持久化测试。
- [ ] 验证关闭学习后不产生新记录，但已有规则仍正常生效。

### 验收标准

- [ ] 连续选择同码次选后，其顺序按设置稳定提升。
- [ ] 关闭调频后立即恢复稳定源顺序或用户硬规则顺序。
- [ ] 删除、固顶和位置规则在重启后保持。
- [ ] 清空用户数据不影响系统 bundle 和内置规则。
- [ ] 小鹤双拼的原有用户模型不与音形模型串用。

## 8. 阶段 4：安全扩展正式数据

### 目标

在不执行原始命令、不开放网络和不携带敏感配置的前提下，逐项接入高价值静态数据。

### 推荐接入顺序

#### 4A：拼字

- [ ] 单独审计 `0.2.拼字.txt`。
- [ ] 定义拼字触发方式、候选隔离和退出行为。
- [ ] 只转换静态文字映射。
- [ ] 覆盖正式主表未收录字、Unicode 扩展区和非法编码测试。

#### 4B：符号和符号组

- [ ] 审计普通符号、符号组和音标数据。
- [ ] 区分静态符号与需要编辑器动作的功能项。
- [ ] 静态文本可进入正式 bundle；动作必须使用受控协议。
- [ ] 防止成对符号重复提交和非 BMP 光标偏移错误。

#### 4C：简繁转换

- [ ] 审计 `hans2hant` 数据及一对多映射。
- [ ] 明确简繁开关、临时繁体和恢复行为。
- [ ] 转换只作用于提交文本，不改变码表编码。
- [ ] 覆盖简繁缺字、一对多和用户词库候选。

#### 4D：静态引导和安全直通

- [ ] 为日期时间、成对符号、光标动作建立白名单协议。
- [ ] 原始 `$cmd`、脚本和平台键码不得直接执行。
- [ ] 每种动作必须映射成明确的类型化动作。
- [ ] 网络、URL、WebDAV、外部程序和动态脚本继续拒绝。

### 验收标准

- [ ] 每类数据拥有独立审计报告、转换合同、哈希和负向样本。
- [ ] 原始来源构建前后保持字节不变。
- [ ] Release HAP 不包含原始 `.txt/.ini`、凭据、路径或审计文件。
- [ ] 损坏或不兼容扩展资源时，基础音形仍可安全使用或明确回退。

## 9. 阶段 5：连续输入和移动端增强

### 目标

补足成熟移动输入法在连续输入场景中的效率，但不把音形后端重新变成不稳定的智能拼音排序。

### 任务

- [ ] 上屏后提供可关闭的关联词。
- [ ] 增加常用短语或诗词补全。
- [ ] 增加英文补全模式，和中文音形状态严格隔离。
- [ ] 增加智能标点开关。
- [ ] 增加明确的撤销、逆切分和恢复上一段能力。
- [ ] 优化外接键盘快符和实体键选候选。
- [ ] 补充剪贴板、emoji、数字和符号子面板的一致返回行为。
- [ ] 所有联想和补全默认本地运行；若未来增加联网能力，必须另立隐私、权限和失败回退设计。

### 验收标准

- [ ] 关联词不会修改已提交文本。
- [ ] 关闭联想后恢复纯码表行为。
- [ ] 中英文、数字、符号和外接键盘状态切换不丢组合。
- [ ] 撤销和逆切分在 UTF-16、emoji、成对符号场景不破坏光标。

## 10. 阶段 6：性能和资源优化

### 当前基线

- 正式 bundle：约 25 MB。
- 五个独立 Release 主机进程加载 P50/P95：约 650/657 ms。
- 逐键查询 P50/P95：约 4.86/18.88 μs。
- 峰值进程内存：约 114 MB。

查询速度已经较快，主要优化目标是加载时间、首键延迟和常驻内存。

### 任务

- [ ] 区分文件读取、完整性校验、反序列化和索引构建耗时。
- [ ] 评估只读内存映射。
- [ ] 评估按分类懒加载或共享只读索引。
- [ ] 压缩重复字符串、分类元数据和索引结构。
- [ ] 避免设置进程与输入法进程重复持有不必要的大对象。
- [ ] 已验证同版本哈希后复用安装结果，避免每次冷启动重复全量工作。
- [ ] 增加首键延迟、方案切换延迟、后台恢复和低内存重建指标。
- [ ] 在 ARM64 真机重新测量，不以主机或模拟器数据代替设备结论。

### 验收标准

- [ ] 逐键查询性能不得明显回退。
- [ ] 首次加载和复用加载分别有 P50/P95。
- [ ] 峰值内存较当前基线有可重复的下降，或有明确的平台限制说明。
- [ ] 优化后 bundle 哈希、候选快照和分类结果保持确定。

## 11. 阶段 7：发布验收

### 任务

- [ ] ARM64 HarmonyOS 物理手机完整验收。
- [ ] 平板、折叠屏、横竖屏和分屏验收。
- [ ] 独立第三方 `TextInput` 应用验收。
- [ ] 密码、邮箱、数字、URL、多行、搜索等编辑器类型验收。
- [ ] 强停、重启、升级安装、损坏资源和低内存恢复验收。
- [ ] 主题、键盘高度、候选模式、分类和学习设置持久化。
- [ ] 长时间输入、快速连击、连续切换方案和翻页压力测试。
- [ ] signed Release HAP 内容白名单、签名、权限和隐私检查。
- [ ] 更新用户说明，准确区分已支持能力和延期能力。

### 发布完成标准

- [ ] 所有纳入本次发布的阶段均有主机、跨层、设备和 Release 证据。
- [ ] 不存在“设置页显示音形但输入法进程仍运行双拼”的跨进程状态错误。
- [ ] 不存在资源损坏后静默加载错误方案的情况。
- [ ] 没有未声明的网络、麦克风、文件或账号权限。
- [ ] ARM64 真机和独立编辑器场景通过后，方可宣称正式发布验收完成。

## 12. 跨阶段门禁

每个阶段都必须执行与变更范围相称的门禁：

- Rust `fmt`。
- Rust workspace 测试。
- Clippy 全 target/feature 且警告视为错误。
- FFI 回归。
- ArkTS 单元测试。
- x86_64 和 arm64-v8a Native 构建。
- Debug/InternalDebug HAP。
- unsigned/signed Release HAP。
- Release 资源正向和负向门禁。
- 正式来源不可变检查。
- 正式生成物确定性双构建。
- 小鹤双拼隔离回归。
- 设置跨进程持久化和失败回滚。

任何阶段如果修改候选规则，都必须同时检查：

- 当前页与完整列表的一致性。
- 用户规则发生在分页之前。
- 分类过滤、唯一性和自动提交使用同一快照。
- 退格、reset、切换方案和重启后的状态。
- 非 BMP 字符和 UTF-16 光标行为。

## 13. 推荐首个实施版本

首个改进版本建议只包含：

1. 一至三码“精确项优先＋前缀项补充”。
2. 四码保持完整精确和唯一自动上屏。
3. 音形编码提示开关。
4. 候选展开面板。
5. 基础查形入口。

暂不同时加入动态调频、拼字、简繁、符号动作和联想。先把音形最核心的“二码看同音、三四码逐步按形过滤”做正确并完成设备验收，再逐阶段叠加其他能力。

## 14. 参考

项目内依据：

- `PROJECT_STATE.md`
- `CHANGELOG.md`
- `engine-rust/crates/code-table-runtime/src/query.rs`
- `engine-rust/crates/code-table-runtime/src/state.rs`
- `engine-rust/crates/ime-engine/src/formal.rs`
- `dictionaries/audit/xiaohe-yinxing/conversion_contract.json`
- `dictionaries/generated/xiaohe-yinxing-production/manifest.json`

公开规则和成熟产品能力参考：

- 小鹤音形官网：<https://flypy.cc/>
- Windows 版更新日志：<https://flypy.cc/win_record.html>
- Android 版更新日志：<https://flypy.cc/android_record.html>

