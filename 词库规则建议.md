有，而且价值很大。但经理发来的这套规则，实际上包含了两种不同能力：

* **小鹤双拼**：双拼编码转拼音，再通过拼音词库和整句解码生成候选。
* **小鹤音形/形码**：直接按照固定码表查词，强调码表顺序、四码自动上屏、顶屏、引导键等规则。

你当前项目已经是一个比较完整的“小鹤双拼输入法”：有 65,122 个正式词条、二进制词库、候选查询、整句解码和用户词频学习。当前使用的是 Rime 简体拼音数据，不是小鹤官方音形码表；底层已经支持用户词库普通新增、`#删/#固/#N`、可靠保存和恢复，但还没有用户管理 UI、分类词库或正式小鹤音形模式。

## 一、这 6 项里，哪些可以用到当前项目

| 经理提出的能力         |       当前项目能否使用 | 建议                  |
| --------------- | -------------: | ------------------- |
| 词条 + TAB + 编码   |       基础已完成 | 已有严格 `FlypyTableImporter` |
| 按码表文件顺序排序       |       基础已完成 | 已有 `source_order` 与 `SourceOrder` |
| `#删/#固/#2` 用户词库 |       底层已完成 | 已有独立 `user-lexicon` 覆盖层 |
| 分类词库与开关         |        尚未实现 | 阶段 11.6 独立码表链路实现 |
| 分号引导键与引导表       |        尚未实现 | 只在独立码表模式启用 |
| 四码自动上屏、清屏、顶屏    |        尚未实现 | 只在独立码表状态机启用 |

前三项已经形成可复用基础，但当前生产小鹤双拼仍保持拼音词库和 `ExistingRanking` 语义。项目已经决定保留双拼链路，并把分类、引导键和固定码长规则放入第二条独立码表链路。

---

# 二、码表格式可以直接接入你的词库构建器

经理给的格式：

```text
词条<TAB>编码
```

例如：

```text
输入法	uurufa
小鹤双拼	xnheulpb
```

你现在已经有：

```text
dictionaries/source/
    ↓
lexicon-builder
    ↓
production.lex
    ↓
Rust 运行时加载
```

因此不需要推倒现有词库系统，只需给 `lexicon-builder` 增加一种新的导入格式：

```text
FlypyTableImporter
```

建议的流程：

```text
小鹤码表 TXT
    ↓
解析 TAB 格式
    ↓
校验词条和编码
    ↓
保留文件行顺序
    ↓
生成项目统一中间结构
    ↓
继续生成 production.lex
```

建议中间结构至少增加：

```rust
struct LexiconSourceEntry {
    text: String,
    code: String,
    category_id: u16,
    category_order: u16,
    source_order: u32,
    flags: EntryFlags,
    fixed_position: Option<u16>,
}
```

这里最重要的是 `source_order`。

因为经理要求候选顺序取决于词条在原始码表里的位置，所以不能再只依赖词频分数。

---

# 三、码表顺序和当前“词频排序”不是一回事

你当前项目的候选排序主要来自：

```text
系统基础词频
＋ 用户选择次数
＋ 最近使用加权
＋ 整句解码评分
```

而经理提出的是：

```text
原始码表出现顺序
```

例如原始码表是：

```text
词条A	unuq
词条B	unan
词条C	unxx
```

输入 `un`，即使 `un` 本身没有对应词条，也要搜索所有以 `un` 开头的后续编码，然后按照文件顺序返回：

```text
词条A
词条B
词条C
```

这意味着查询层要区分两类结果：

```text
Exact Match：编码完全相同
Prefix Match：编码以当前输入开头
```

建议规则为：

```text
1. 完全匹配候选优先
2. 没有完全匹配时，返回前缀后续候选
3. 前缀候选按照 sourceOrder 排序
4. 不允许 HashMap 或并行构建破坏原始顺序
```

你的 `candidate-query` 已经支持前缀查询，但要额外确认：构建二进制词库后，原始码表顺序是否仍然被保留。不能使用普通哈希遍历顺序来输出候选。

---

# 四、`#删/#固/#2` 非常适合做成用户词库覆盖层

这是经理需求中，对你项目最有价值的一部分。

你现在已有的 `user-model` 是“用户词频学习”：

```text
用户多次选择某个候选
→ 增加权重
→ 候选逐渐提前
```

但这不等于用户词库。

例如：

```text
测试词	abc#固
```

它代表明确的人工规则，不应该随着学习权重变化。

因此不要把 `#删/#固/#2` 塞进现有的 `user-model`，建议新增一个独立模块：

```text
engine-rust/crates/user-lexicon/
```

两者职责应当分开：

```text
user-model
负责自动学习、选择次数、最近使用时间

user-lexicon
负责人工添加、删除、固定位置、词库导入导出
```

## `#删`

格式：

```text
词条	编码#删
```

实现上不要真的修改系统二进制词库，而是建立“删除标记”：

```rust
HashSet<(Code, Text)>
```

查询时：

```text
系统词库查询
→ 检查删除标记
→ 被标记的词条不进入候选
```

这样用户删除词条后可以恢复，也不需要重新构建整个系统词库。

## `#固`

格式：

```text
词条	编码#固
```

作用：

```text
固定在同码候选前面
多个 #固 按用户词库中的出现顺序排列
不受用户学习权重影响
```

建议候选优先级：

```text
#固词条
→ 普通用户词条
→ 系统词条
```

或者严格按经理要求：

```text
所有 #固 词条在最前
多个 #固 按用户文件顺序
其余候选继续按码表顺序
```

## `#2`

格式：

```text
词条	编码#2
```

含义不是“增加一个排序分数”，而是：

```text
把这个词条插入候选第 2 位
```

因此不能简单使用：

```rust
score += 1000
```

应该采用候选列表合并算法：

```text
1. 取得系统候选
2. 过滤 #删
3. 放置 #固
4. 按指定位置插入 #N
5. 填充剩余候选
6. 最后对没有固定位置的候选应用用户学习
```

建议明确规定：

```text
#固 和 #N 属于硬排序规则
用户词频学习属于软排序规则
软排序不得突破硬排序位置
```

否则用户多选几次后，`#2` 可能又变成第 1 或第 5，违背用户配置。

---

# 五、分类词库非常适合你当前设置架构

你已经有完整的设置模型、Preferences 持久化和设置即时生效机制，所以分类词库开关主要是扩展设置模型，不需要重新设计设置系统。

例如可以有：

```text
基础词库：始终开启
常用词库：开启
成语词库：开启
网络词语：关闭
计算机词库：开启
医学词库：关闭
地名词库：开启
```

配置可以设计成：

```typescript
interface LexiconCategorySettings {
  enabledCategoryIds: string[]
  categoryOrder: string[]
}
```

Rust 词条增加：

```rust
category_id: u16
```

查询时执行：

```text
过滤关闭的分类
→ 按分类列表顺序
→ 分类内部按码表顺序
→ 应用用户词库硬规则
→ 应用允许范围内的用户学习
```

最终排序可以明确成：

```text
1. 删除规则
2. 固顶规则
3. 指定位置规则
4. 分类词库顺序
5. 分类内部码表顺序
6. 用户学习软调整
```

还要处理同一个词条同时存在于多个分类的问题。建议去重时保留优先级最高的分类来源。

---

# 六、分号引导键可以做，但应当放在形码模式

经理提到：

```text
分号作为引导键
＋
引导表词库
```

这通常意味着：

```text
用户按 ;
→ 输入法进入引导状态
→ 后续编码不查询普通词库
→ 改为查询引导表
```

例如可以用于：

* 特殊符号；
* 日期时间；
* 常用短语；
* Emoji；
* 特殊字；
* 快捷输入；
* 拼音反查；
* 音形辅助表。

建议 Rust 状态机增加：

```rust
enum CompositionMode {
    Normal,
    Guide,
}
```

处理流程：

```text
普通状态按下 ;
    ↓
进入 Guide 模式
    ↓
显示引导标记
    ↓
后续按键查询 guide.lex
    ↓
选择候选或删除到空
    ↓
退出 Guide 模式
```

需要注意：分号本来也是标点，所以只有在满足条件时才作为引导键。例如：

```text
中文音形模式
＋
组合编码为空
＋
引导功能开启
```

在英文、数字、URL、邮箱等布局中，分号仍然应该直接上屏，不进入引导状态。

---

# 七、三个“四码参数”不能直接加入当前双拼输入模式

这是最需要注意的地方。

你现在的输入法支持：

```text
nihc → 你好
uurufa → 输入法
xnheulpb → 小鹤双拼
```

连续双拼输入长度可能是：

```text
2、4、6、8、10……
```

如果全局启用：

```text
自动上屏码长 = 4
```

那么用户输入 `uurufa` 时，输入到前四个字母 `uuru` 就可能自动上屏，后面的 `fa` 会被拆开。

这会直接破坏你已经完成的：

* 连续双拼；
* 多音节输入；
* 整句解码；
* 部分候选提交；
* 剩余编码继续转换。

所以三个参数必须属于“形码方案配置”，不能成为全局输入法配置。

建议增加方案行为类型：

```rust
enum SchemeKind {
    ShuangpinPinyin,
    FixedLengthCodeTable,
}
```

配置示例：

```json
{
  "id": "xiaohe_shuangpin",
  "kind": "shuangpin_pinyin",
  "autoCommitLength": null,
  "emptyClearLength": null,
  "overflowCommitLength": null
}
```

```json
{
  "id": "xiaohe_yinxing",
  "kind": "fixed_length_code_table",
  "maxCodeLength": 4,
  "autoCommitLength": 4,
  "emptyClearLength": 4,
  "overflowCommitLength": 4,
  "guideKey": ";"
}
```

## 自动上屏码长 4

建议逻辑：

```text
当前编码长度达到 4
＋
候选唯一
＋
不存在需要继续输入才能区分的后续编码
→ 自动提交唯一候选
```

该判断必须同时检查：

```text
exactCandidateCount
descendantCodeCount
```

不能只看候选栏当前是否只有一个候选，因为分页、过滤和分类关闭都可能造成“看起来只有一个”。

## 空码清屏码长 4

建议逻辑：

```text
编码长度达到 4
＋
没有完全匹配候选
＋
没有任何以该编码开头的后续编码
→ 清空组合编码
```

也就是同时满足：

```text
exactCandidateCount == 0
prefixDescendantCount == 0
```

如果没有完全匹配，但是存在后续编码，就不能清空。

## 顶屏码长 4

建议逻辑：

```text
已有 4 码组合
用户输入第 5 个按键
    ↓
先提交前 4 码首选候选
    ↓
把第 5 个按键作为新组合的第 1 码
    ↓
重新查询候选
```

执行结果类似：

```text
abcd + e
→ 提交 abcd 的首选
→ 当前组合变成 e
```

这需要由 Rust 引擎一次返回：

```typescript
{
  commitText: "前四码首选",
  rawInput: "e",
  candidates: [...]
}
```

不要在 ArkTS 中先提交、再单独调用一次 `processKey`，否则快速输入时容易再次出现异步乱序。

---

# 八、建议新增统一“方案行为配置”

目前你的 `xiaohe.json` 主要描述声母、韵母和双拼解析规则。

可以扩展为两部分：

```json
{
  "id": "xiaohe",
  "parser": {
    "type": "shuangpin",
    "initials": {},
    "finals": {}
  },
  "behavior": {
    "candidateOrder": "frequency",
    "prefixFallback": true,
    "guideKey": null,
    "maxCodeLength": null,
    "autoCommitLength": null,
    "emptyClearLength": null,
    "overflowCommitLength": null
  }
}
```

未来音形方案则是：

```json
{
  "id": "xiaohe_yinxing",
  "parser": {
    "type": "code_table"
  },
  "behavior": {
    "candidateOrder": "table_order",
    "prefixFallback": true,
    "guideKey": ";",
    "maxCodeLength": 4,
    "autoCommitLength": 4,
    "emptyClearLength": 4,
    "overflowCommitLength": 4
  }
}
```

这样可以避免在引擎代码里写大量：

```rust
if scheme == "xiaohe_yinxing" {
    ...
}
```

---

# 九、对你当前项目最合理的实施顺序

## 第一阶段：码表顺序基础

状态：**已完成底层基础**。

先实现：

```text
词条 + TAB + 编码导入
sourceOrder 保留
同码按原始顺序
空码前缀候选
确定性二进制构建
```

这部分既能服务双拼，也能服务未来形码。

## 第二阶段：用户词库覆盖层

状态：**已完成底层与正式双拼覆盖接入**。

实现：

```text
用户词库加载
#删
#固
#N
原子保存
损坏恢复
导入校验
候选硬排序
```

底层解析、硬规则、可靠保存和运行时覆盖已经完成；仍缺少面向用户的管理、导入导出和文件选择界面。

## 第三阶段：分类词库

状态：**阶段 11.6 计划，尚未实现**。

实现：

```text
categoryId
分类列表顺序
分类开关
设置持久化
运行时重新加载或过滤
```

## 第四阶段：小鹤音形模式

状态：**阶段 11.6 计划，尚未实现；正式码表来源和分发授权待确认**。

再实现：

```text
固定四码状态机
分号引导键
引导表
自动上屏
空码清屏
顶屏
音形专用码表
```

不要把第四阶段混进现有双拼引擎的小修改里，否则很容易破坏已通过验收的整句输入功能。

---

# 十、当前项目中需要特别调整的模块

建议修改范围：

```text
engine-rust/
├─ crates/
│  ├─ lexicon-core/
│  │  └─ 增加来源顺序、分类、标记等元数据
│  ├─ candidate-query/
│  │  └─ 增加稳定前缀枚举
│  ├─ candidate-ranking/
│  │  └─ 增加硬排序与软排序边界
│  ├─ user-model/
│  │  └─ 保持现有自动学习，不负责 #删/#固
│  ├─ user-lexicon/
│  │  └─ 复用已有用户码表覆盖层
│  ├─ ime-state/
│  │  └─ 增加普通、引导、固定码长状态
│  └─ ime-engine/
│     └─ 根据 SchemeKind 选择行为
│
├─ tools/lexicon-builder/
│  └─ 增加 FlypyTableImporter
│
└─ schemas/
   ├─ xiaohe.json
   └─ xiaohe_yinxing.json
```

ArkTS 主要增加：

```text
设置中的分类词库开关
用户词库导入导出入口
形码方案选择
三个码长参数
引导功能开关
```

C++ 仍然只负责参数和结果转换，不应该加入 `#固`、四码顶屏或词库排序逻辑。项目计划也明确规定候选排序、词库索引和状态机必须留在 Rust 层。

---

## 最终判断

经理发来的内容不只是“优化一下现有词库”，而是在描述一个比较完整的**码表输入法引擎**。

对你当前项目来说，以下基础已经完成：

* `TAB 码表格式`
* `码表顺序`
* `#删/#固/#N`

前三项已经落地；`分类词库` 尚未实现。它们将作为独立码表后端的基础继续复用，不再把系统码表顺序强行混入现有双拼候选排序。

而：

* `分号引导`
* `自动上屏`
* `空码清屏`
* `五码顶屏`

应当作为“小鹤音形/固定四码方案”的独立行为，不能全局应用到现有小鹤双拼，否则会破坏连续双拼和整句输入。该双后端决策已写入 `项目计划书.md` 阶段 11.6；代码尚未开始实现。

另外，技术上可以研究和接入小鹤官方码表，但正式打包发布前必须确认码表的授权范围。你的项目计划明确要求第三方词库来源和许可证必须可追溯，不能因为文件可以下载就默认可以随应用再分发。
