# Project State

The canonical current project state is maintained at the repository root:

```text
PROJECT_STATE.md
```

This file exists only as a docs-directory pointer so the project does not maintain two divergent state records.
