# 小鹤音形精准匹配：阶段 0 基线报告

日期：2026-08-04  
结论：`阶段 0：PASS（主机可执行范围）`

## 完成范围

本阶段确认精准匹配核心合同，新增 ADR 0022、可重复生成器、当前渐进候选快照、小鹤双拼隔离快照、主机 Release 性能基线和 CI 门禁。没有修改生产查询、状态机、FFI、C++、ArkTS UI、正式码表或正式资源。

## 正式资源与权限

| 字段 | 冻结值 |
| --- | --- |
| scheme / bundle | `xiaohe-yinxing` / `xiaohe-yinxing-production` |
| bundle 大小 | `25,397,952` bytes |
| bundle SHA-256 | `00c7d5a9d6b74a079a7434df23f510aa348fe8fee5bf1eaf72e691d68bcd1e30` |
| 普通记录 | `73,263` |
| 内置规则 | `36` |
| 分类 | 8 类；`core` 必选，默认全部启用 |
| 应用权限 | 仅 `ohos.permission.VIBRATE`；无 `INTERNET` |

分类记录数仍为 `68,505 / 1,690 / 26 / 66 / 362 / 464 / 498 / 1,652`，与正式 manifest 一致。生成器在写入基线前强制检查 bundle 大小与 SHA-256，正式数据未修改。

## 当前渐进行为

页大小为 50，正式音形快照上限为 512。两条逐键路径冻结结果如下：

| 路径 | 一码 | 二码 | 三码 | 四码 |
| --- | --- | --- | --- | --- |
| `aaba` | `a`：50 项当前页 | `aa`：41 项 | `aab`：4 项 | 唯一提交“阿爸”，组合清空 |
| `jumk` | `j`：50 项当前页 | `ju`：50 项当前页 | `jum`：13 项 | 5 个完整码候选，保持组合 |

十个既有二码全局渐进候选数继续为：`aa 41`、`ai 115`、`an 165`、`ni 94`、`hc 209`、`ui 343`、`vi 353`、`wo 68`、`xm 264`、`xq 135`。快照保存第一页完整候选字段和分页拼接后的全部稳定 ID。

专项状态：

- `aaba`：唯一四码提交“阿爸”一次并清空。
- `jumk`：5 个完整重码，不自动提交。
- `jumke`：先提交“驹”，再把 `e` 恰好作为新段第一码，当前页 50 项。
- `aaa`：无候选但保留可恢复组合，不提交旧候选。
- `aaaa`：按既有反向空码切分提交“阿”，保留 `aa` 及其 41 个候选。
- 用户 `#删/#固/#N`、分类过滤和四码精确路径继续引用 2026-07-27 不可变快照；四码在当前渐进策略下显式退化为 `ExactOnly`。

## 小鹤双拼隔离

重新生成的当前快照覆盖 `nihc → 你好`、`uurufa → 输入法`、`xnhe → 小鹤`、`ulpb → 双拼` 的逐键状态、当前候选、翻页探针、选择提交、退格、reset、重建会话和从音形切回双拼。候选与双拼快照各连续生成两次，逐字节一致。

确定性哈希：

- 渐进候选：`fa0486f24f7ce72ded70f1c8347e1ad3a8960068bbc98c4793ccc88fd021eb2b`
- 小鹤双拼隔离：`b6741d6d229f2f0eab9bb787b5d06c22dec47a29510ec6b4253aa7210698c5e3`

## 主机 Release 性能

环境为当前 Windows x86_64 主机，3 次预热、10 个样本；文件系统缓存未控制。这些结果用于改造前 A/B，不是跨机器阈值。

| 指标 | P50 | P95 | 最大值 |
| --- | ---: | ---: | ---: |
| 音形 `h` 冷状态查询 | 1.2965 ms | 1.4200 ms | 1.6102 ms |
| 音形 `h` 热状态查询 | 0.9870 ms | 1.1432 ms | 1.1722 ms |
| 双拼 `h` 冷查询 | 2.4079 ms | 2.4773 ms | 2.5946 ms |
| 双拼 `h` 热查询 | 0.0409 ms | 0.0483 ms | 0.0542 ms |

- 音形 `h` 完整候选快照：512 项；50 项当前页协议 JSON：5,057 bytes。
- 双拼 `h` 完整候选快照：256 项；50 项当前页协议 JSON：4,931 bytes。
- 候选快照采样后工作集增量：8,560,640 bytes。
- 进程峰值工作集：148,779,008 bytes。
- Phone/Pad UI 可见延迟和内存、ARM64 物理真机性能：`NOT_RUN`。

## 门禁

| 门禁 | 结果 |
| --- | --- |
| 正式 bundle 大小/SHA-256 | PASS |
| 当前渐进候选双生成逐字节比较 | PASS |
| 当前小鹤双拼双生成逐字节比较 | PASS |
| `candidate-baseline` 单元测试 | 4/4 PASS |
| 精准匹配阶段 0 专项测试 | PASS |
| 2026-07-27 四码/用户规则旧基线测试 | PASS |
| Rust workspace 全量测试 | 423/423 PASS |
| ArkTS 全量测试 | BUILD SUCCESSFUL；既有警告未新增 |
| `cargo fmt --check` | PASS |
| 全 workspace/all-target/all-feature Clippy | PASS |

统一证据入口为 `scripts/generate-xiaohe-yinxing-precise-match-stage0.ps1`，机器文件位于 `dictionaries/audit/xiaohe-yinxing/precise-match-stage0/`。

阶段 0 没有设备 UI 变更，因此不复用现有模拟器证据宣称本轮设备验收；Phone、Pad、ARM64 和第三方输入框留给后续运行时/UI 阶段。
