# 阶段 11 设备证据

`scripts/device-accept-stage11.ps1` 已在 2026-07-14 的 x86_64 HarmonyOS API 24 模拟器上使用最新 Debug HAP（SHA-256 `24DB8D55639934E98C865E367894D1F42303F9AA1523C447C475F8EB01625417`）返回 `STAGE11_DEVICE_ACCEPTANCE_RESULT=PASS`，并将设置首页布局、截图、重启布局、学习开关和真实模型清空前后证据写入本目录。

候选栏重叠修复的几何证据位于 `stage11_clear_candidate.json`：候选内容区边界为 `[63,1704][949,1883]`，右侧分页区边界为 `[949,1704][1257,1883]`；两者仅在 `x=949px` 处相接，没有面积交集。候选项点击区域最右到 `x=928px`，上一页、页码和下一页区域均完整位于分页区内。

模拟器结果证明页面、设置链路、持久化重载和清空模型链路；不代表 ARM64 设备的真实震感或扬声器效果。
