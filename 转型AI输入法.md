# 转型 AI 输入法

> 文档状态：产品与技术路线规划，不代表相关能力已经实现
>
> 适用基线：双羽输入法 `0.4.0` 及当前主干
>
> 更新日期：2026-08-21

---

## 1. 文档目的

本文档用于指导双羽输入法从“功能完整、稳定可用的普通输入法”逐步转型为“本地优先、用户可控、可选云端增强的 AI 输入法”，并把语音识别纳入同一套输入会话、隐私、安全、性能和验收体系。

转型的核心结论是：

> 不替换现有确定性输入引擎，而是在其上增加独立、可关闭、可降级的 AI 与语音辅助层。

普通拼音、双拼、音形、9 键、候选、用户词库和直接控制能力仍然是产品根基。AI 负责减少输入步骤、改善表达和提供可选建议，不能成为基本打字的前置依赖，也不能在用户不知情的情况下读取、上传或改写文本。

---

## 2. 当前项目基础与转型条件

当前项目已经具备转型 AI 输入法所需的大部分底层能力：

- HarmonyOS `InputMethodExtensionAbility -> ArkTS -> C++ Node-API -> Rust C ABI` 完整链路。
- 小鹤双拼、全拼、9 键拼音和小鹤音形多输入方案。
- 正式词库、候选召回、候选排序、整句解码、分页和部分提交。
- 本地用户学习、用户词库覆盖、固定候选和分类词库。
- 编辑器类型识别、密码场景保护、候选栏显隐和学习开关。
- 输入会话 `generation`、预览文本、提交历史、安全撤销和光标前文本核验。
- 全拼上下文 n-gram 重排能力，以及可校验、可限时、可失败关闭的本地模型装载机制。
- 无网络依赖、日志脱敏、Release 内容门禁和较完整的主机/设备验收体系。

因此，下一阶段不应重写输入引擎，也不应立即把大语言模型塞进逐键候选链路。最合理的演进方式是复用现有边界，增加 AI Provider、语音识别 Provider、隐私策略和新的候选表面。

### 2.1 当前需要补齐的基础能力

- 在输入法 Ability 创建时查询并保存 HarmonyOS 输入法安全模式。
- 建立 AI 请求、AI 候选、语音会话和能力状态的稳定领域协议。
- 将普通候选、关联候选、AI 候选和语音结果在 UI 与状态模型中明确区分。
- 建立云端 API 代理服务，客户端不得内置永久大模型密钥。
- 建立麦克风权限引导、语音识别生命周期和 ARM64 真机验收流程。
- 为 AI/语音增加独立隐私说明、设置开关、失败回退和内容门禁。

---

## 3. 产品定位

建议产品定位为：

> 普通输入始终可靠，AI 随时可用但不打扰；文字和语音都尽量在本地处理，任何云端处理都由用户主动触发并清楚可见。

差异化不应只是“接入了某个大模型”，而应体现在输入法特有的低摩擦交互：

- AI 建议出现在输入流程中，不要求用户离开当前应用。
- 建议可以一次接受、逐段接受、拒绝或撤销。
- AI 不破坏小鹤音形等确定性输入语义。
- AI 关闭、离线、超时或不可用时，普通输入完全正常。
- 语音识别结果先作为可确认的输入内容，而不是不可逆地写入编辑器。
- 用户可以明确知道某项能力是本地处理还是云端处理。

---

## 4. 总体原则

### 4.1 普通输入优先

- 逐键输入不得等待网络或生成式模型。
- AI 初始化失败不得导致引擎、键盘或候选栏不可用。
- 默认候选顺序不能被不透明的生成结果替换。
- 关闭 AI 后恢复纯本地、确定性的当前行为。

### 4.2 本地优先、云端可选

- 候选排序、用户学习、短语联想和基础纠错优先本地完成。
- 改写、润色、扩写和复杂回复在第一版通过云端 API 验证价值。
- 端侧生成模型只在真机性能、包体、功耗和兼容性达标后逐步引入。

### 4.3 用户主动、结果可逆

- 生成式 AI 必须由用户点击明确动作触发。
- AI 结果先展示为候选，不自动执行替换。
- 写入前校验输入会话、光标位置和目标文本。
- 写入后接入现有撤销链路。

### 4.4 隐私分级

- 密码、验证码、支付、屏幕锁等敏感编辑器完全禁用 AI、语音和学习。
- 本地 AI 与云端 AI 必须拥有不同状态和 UI 标识。
- 不默认读取完整编辑框，不采集应用包名、联系人、剪贴板或账号信息。
- 日志只记录长度、动作类型、阶段、耗时和脱敏错误码。

### 4.5 AI 不拥有执行权限

- AI 只能返回普通文本候选。
- 模型输出不得直接映射为直通命令、文件操作、网络操作或系统意图。
- 即使生成内容中包含类似 `$cmd`、URL 或协议字符串，也只作为普通文本处理。

---

## 5. 目标能力分层

### 5.1 第一层：本地即时智能

这部分可以在普通输入时持续工作，但必须保持低延迟和有界资源：

- 下一词和下一短语联想。
- 已上屏内容与当前候选之间的上下文重排。
- 中英文混输、数字、日期和常用实体格式优化。
- 拼音纠错、漏字提示和标点建议。
- 用户高频短语与本地用户词库推荐。
- 简单场景分类，例如搜索、聊天和长文本编辑器，但不依赖应用包名。

第一阶段优先扩展现有 Rust `context-reranker` 和候选系统，不引入大语言模型。

### 5.2 第二层：用户主动调用的生成式 AI

第一版建议提供以下动作：

- 续写。
- 更简洁。
- 更礼貌。
- 更正式。
- 扩写。
- 中英互译。
- 生成三条回复建议。

第一版只处理“最近一次由本输入法上屏的文字”或“当前仍由输入法拥有的组合文本”。暂不默认读取整个输入框，也不承诺处理任意应用中的选区。

### 5.3 第三层：语音输入

语音输入负责将实时语音转换为可确认、可编辑、可撤销的文字：

- 中文普通话和中文语境下的英文混说。
- 实时识别中间结果展示。
- 停止、取消、重说和确认上屏。
- 识别结果的本地标点整理。
- 后续可选 AI 去口语化、摘要或格式化。

### 5.4 第四层：个性化智能

后续可以增加：

- 用户常用表达风格。
- 行业术语与个人词库协同。
- 用户对 AI 候选的接受/拒绝偏好。
- 不同输入场景的候选风格偏好。
- 本地可查看、可清除、可导出或可关闭的个性化数据。

---

## 6. 总体架构

```text
Keyboard / CandidateBar / VoiceInputBar
  -> InputSessionController
     -> EditorPrivacyPolicy
     -> AiAssistController
        -> LocalSuggestionProvider
        -> LocalModelProvider（后续）
        -> CloudAiProvider（可选）
     -> VoiceInputController
        -> CoreSpeechRecognitionProvider
        -> LocalAsrProvider（后续）
     -> VerifiedTextReplacer
     -> ImeConnectionService

普通输入：
Keyboard -> EngineCoordinator -> C++ -> Rust IME Engine

本地 AI：
AiAssistController -> Rust context-reranker / 可选端侧推理

云端 AI：
AiAssistController -> HTTPS -> 自有业务服务 -> 大模型供应商

语音：
VoiceInputController -> Core Speech Kit -> 中间/最终识别结果
```

### 6.1 边界要求

- Rust 输入引擎仍是拼音、音形、候选和用户学习的权威实现。
- ArkTS 负责 UI、输入会话、平台能力、权限和 Provider 调度。
- C++ 继续只做跨语言和端侧推理适配，不承载业务策略。
- 云端 Provider 不进入 Rust 逐键处理函数。
- AI 与语音都必须绑定 `InputSessionStore` 的单调 `generation`。
- 会话停止、键盘隐藏、编辑器切换或用户继续输入时，旧异步结果不得写入新会话。

---

## 7. 本地模型、云端 API 与混合路由

### 7.1 三种实现方式比较

| 方式 | 适合能力 | 优点 | 主要问题 | 第一版结论 |
| --- | --- | --- | --- | --- |
| Rust 规则/n-gram | 关联词、排序、纠错 | 快、稳定、离线、资源小 | 生成能力有限 | 必须使用 |
| 端侧小模型 | 分类、短预测、部分改写 | 隐私好、无网络延迟 | 包体、内存、兼容性、质量 | 只做 POC |
| 云端大模型 API | 改写、续写、翻译、回复 | 质量高、迭代快 | 网络、费用、隐私、安全模式 | 用户主动开启 |

### 7.2 推荐路由

```text
if 敏感编辑器:
    禁用 AI 与语音
else if 输入法安全模式 == BASIC:
    仅本地候选与本地智能
else if 用户主动触发生成动作 and 已同意云端处理:
    调用 CloudAiProvider
else:
    使用 LocalSuggestionProvider
```

### 7.3 为什么第一版生成式能力先调用 API

- 可以先验证“改写、续写、回复”是否真的提高输入效率。
- 不必在第一版承担大型端侧模型的包体和适配成本。
- 模型质量、提示词和候选格式可以由服务端快速调整。
- 可以通过统一服务端切换供应商，避免客户端与某一模型强绑定。

客户端不得直接保存永久 API Key。推荐链路是：

```text
输入法 -> 自有业务服务器 -> 大模型 API
```

自有服务器负责鉴权、限流、配额、费用控制、模型切换、超时、输出校验和隐私策略。第一版使用普通短请求即可，不必为了少量短候选引入复杂的流式协议。

### 7.4 云端请求最小合同

建议第一版只发送：

```json
{
  "requestId": "随机请求标识",
  "action": "POLITE",
  "text": "用户明确提交给 AI 的文本",
  "language": "zh-CN",
  "maxSuggestions": 3
}
```

不得默认发送：

- 完整编辑框。
- 应用包名或应用标题。
- 联系人、剪贴板、账号和设备广告标识。
- 原始按键序列、用户词库和长期输入历史。
- 与当前动作无关的前后文。

服务端返回严格结构：

```json
{
  "requestId": "原请求标识",
  "suggestions": [
    { "text": "建议一", "label": "礼貌" },
    { "text": "建议二", "label": "简洁" }
  ],
  "providerVersion": "服务端模型版本"
}
```

候选数、单条长度、总响应字节数和字符类型必须有硬上限。任何无法解析、超长、空响应或协议不匹配都按失败处理。

### 7.5 端侧模型的后续接入

端侧模型建议通过 C/C++ 对接 HarmonyOS MindSpore Lite Kit 或 Neural Network Runtime Kit，模型资源必须：

- 独立版本和哈希。
- 懒加载，不参与普通输入冷启动。
- 可选安装或按设备能力分级。
- 损坏、超时或资源超限时失败关闭。
- 不改变 Rust 输入引擎的确定性候选合同。

如果未来由主应用下载可选模型，应通过经过审批的共享沙箱向输入法扩展提供只读模型，并校验签名、哈希、版本、大小和兼容范围。基础安全模式下不得尝试让输入法扩展自行联网下载。

---

## 8. 第一版产品范围

建议当前 `0.4.0` 之后的第一版 AI/语音版本定义为拟议 `0.5.0`。版本号仅为规划建议，实施时可根据发布节奏调整。

### 8.1 第一版必须包含

#### AI 基础

- `AiProvider`、请求协议、候选协议和错误码。
- `LocalSuggestionProvider` 与 `CloudAiProvider` 的统一调度边界。
- 安全模式、编辑器类型、用户设置和会话状态四重门禁。
- AI 请求取消、超时、陈旧结果拒绝和失败回退。
- AI 输出只作为普通文本候选。

#### AI 用户功能

- 候选栏空闲状态显示 `AI` 或 `✨` 入口。
- 对最近一次上屏文本提供“续写、简洁、礼貌、正式、翻译”。
- 每次最多展示 3 条建议。
- 点击候选前再次核验最近上屏文本仍位于光标前。
- 替换失败时不删除原文；必要时允许用户选择“仅插入”。
- 成功替换后支持一次安全撤销。

#### 本地智能

- 上屏后的可关闭关联词。
- 关闭关联词后恢复当前候选行为。
- 关联候选不得自动修改已提交文字。
- 继续使用有界候选池、确定性排序和本地用户规则。

#### 语音输入

- 键盘提供麦克风入口和明确的录音状态。
- 支持开始、停止、取消、重说和确认上屏。
- 支持实时中间结果和最终结果。
- 第一版语言以 `zh-CN` 为主，支持中文语境下的英文混说能力探测。
- 默认单次最长 20 秒，产品硬上限不超过平台短语音 60 秒限制。
- 原始音频不落盘、不进入日志、不进入用户学习模型。
- 识别完成后文字进入语音候选区，用户确认后再写入编辑器。
- 键盘隐藏、会话停止、应用切换、权限撤回或音频中断时立即取消并释放资源。

### 8.2 第一版建议暂不包含

- 每次按键调用云端大模型。
- 在 Release HAP 中内置大型生成式模型。
- 后台持续监听或语音唤醒。
- 默认读取整个编辑框或跨应用聊天历史。
- AI 自动发送消息、自动执行命令或自动操作其他应用。
- 任意选区的复杂跨宿主替换。
- 会议级长录音和 8 小时转写。
- 说话人分离、情绪识别和声纹识别。
- 语音识别后静默进行云端润色。
- 云端保存用户输入用于训练。

### 8.3 第一版推荐用户流程

#### AI 改写

```text
用户正常输入并上屏
  -> 点击 AI
  -> 选择“更礼貌”
  -> 显示“云端处理”标识
  -> 返回最多 3 条建议
  -> 用户点击建议
  -> 核验原文与会话
  -> 安全替换
  -> 可撤销
```

#### 语音输入

```text
用户点击麦克风
  -> 检查编辑器、完整体验模式和麦克风授权
  -> 创建/复用语音识别引擎
  -> 开始录音并显示状态
  -> 中间文本显示在 VoiceInputBar
  -> 用户停止或 VAD 判定结束
  -> 显示最终文本
  -> 用户确认上屏 / 重说 / 取消
```

#### 基础安全模式

```text
普通输入、本地学习、本地关联正常
云端 AI 入口隐藏或显示为不可用说明
语音入口显示“需要完整体验模式”说明
不反复请求麦克风或网络能力
```

---

## 9. 语音识别详细设计

### 9.1 第一版技术选择

第一版优先使用 HarmonyOS Core Speech Kit 的 `speechRecognizer` 能力，不自行集成完整 ASR 模型。官方能力支持 PCM 文件或实时音频转文字，短语音模式上限为 60 秒；官方示例使用 16 kHz、单声道、16 bit PCM，并以固定长度音频块写入识别引擎。

必须注意：

- 官方文档明确语音识别能力不支持模拟器，必须使用真实手机和平板验收。
- `online` 模式、无网能力、支持语言和实际设备覆盖必须通过目标 API 24 真机探针确认，不能仅根据示例参数推断。
- 如果实际模式需要云端服务，产品必须把它按云端语音处理展示并取得用户同意。

### 9.2 输入法安全模式限制

HarmonyOS 输入法基础模式会限制麦克风、网络和向进程外传递数据。因此：

- 基础模式下语音输入不可用，即使用户已授予普通麦克风权限也不能假设可用。
- 输入法 Ability 创建时应调用平台 `getSecurityMode`，将结果写入只读运行时能力状态。
- 完整体验模式下，仍需检查并动态获得 `ohos.permission.MICROPHONE`。
- 安全模式发生变化后，应在下一会话重新探测，不持久化伪造的“可用”状态。

### 9.3 权限与设置

项目当前正式权限只有 `ohos.permission.VIBRATE`。加入语音后需要显式增加：

- `ohos.permission.MICROPHONE`，并配置用途说明和 `inuse` 使用场景。
- 如果云端 AI 或在线语音由输入法扩展直接访问，还需要对应网络能力；但应先完成安全模式和审核方案，不得直接删除现有 Release 的 INTERNET 负向门禁。

推荐在主应用设置页完成语音说明和权限引导，键盘只检查能力并提供进入设置的明确路径。用户拒绝权限后，不应每次点击都反复弹窗。

建议新增设置：

- `voiceInputEnabled`：语音功能总开关。
- `voiceLanguage`：第一版固定或默认 `zh-CN`。
- `voiceAutoCommit`：第一版默认关闭。
- `voiceCloudConsentVersion`：记录用户已阅读的云端语音说明版本，不记录语音内容。
- `voicePunctuationEnabled`：是否使用识别服务或本地规则补标点。

### 9.4 语音状态机

```text
IDLE
  -> PREPARING
  -> LISTENING
  -> FINALIZING
  -> RESULT_READY
  -> COMMITTING
  -> IDLE

任意活动状态
  -> CANCELING
  -> IDLE

任意状态
  -> ERROR
  -> IDLE / RETRY
```

状态必须记录 `sessionId`、输入会话 `generation` 和引擎实例代次。旧语音回调不得更新新输入会话。

### 9.5 中间结果与最终结果

- 中间结果只显示在专用 `VoiceInputBar`，第一版不反复写入宿主编辑器。
- 最终结果显示“上屏、重说、取消”三个明确动作。
- 用户确认后使用现有 `ImeConnectionService.insertText` 或兼容的预览提交链路一次写入。
- 上屏成功后进入 `CommitHistoryManager` 和 `UndoCommitManager`。
- 写入失败时保留语音结果，允许复制、重试或取消；不得显示虚假成功。

后续可以在明确支持文本预览的编辑器中把中间结果作为组合预览，但必须绑定现有 preview ownership 与 generation，并为不支持预览的宿主保留 VoiceInputBar 降级路径。

### 9.6 语音与 AI 的关系

第一版保持两个步骤明确分离：

```text
语音 -> 识别文字 -> 用户确认
用户再次选择 AI 动作 -> 可选润色/摘要/去口语化
```

不得把原始音频发送给生成式文本模型，也不得在用户没有选择 AI 动作时自动把语音识别文本发送到第二个云服务。

后续可以增加：

- 一键去除“嗯、啊、然后”等口头语。
- 自动补全标点和分段。
- 语音生成待办、日程和结构化笔记，但只生成文本，不直接执行系统动作。
- 本地领域热词注入，例如用户词库、人名和专业术语；前提是 Core Speech Kit 或后续 ASR Provider 明确支持安全热词接口。

---

## 10. 建议新增的工程模块

### 10.1 ArkTS 领域与应用层

建议新增：

```text
entry/src/main/ets/domain/ai/AiContracts.ets
entry/src/main/ets/domain/voice/VoiceContracts.ets
entry/src/main/ets/domain/security/InputMethodCapabilityState.ets

entry/src/main/ets/application/AiAssistController.ets
entry/src/main/ets/application/VoiceInputController.ets
entry/src/main/ets/application/VerifiedTextReplacer.ets
```

职责：

- `AiContracts`：动作、请求、建议、来源、状态和错误码。
- `VoiceContracts`：语音会话、识别结果、状态和错误码。
- `InputMethodCapabilityState`：安全模式、麦克风授权、本地/云端能力快照。
- `AiAssistController`：上下文裁剪、Provider 路由、超时、取消和候选更新。
- `VoiceInputController`：权限、录音、识别引擎、回调代次和结果提交。
- `VerifiedTextReplacer`：替换前核验、原子删除/插入和撤销记录。

### 10.2 ArkTS 基础设施层

建议新增：

```text
entry/src/main/ets/infrastructure/ai/AiProvider.ets
entry/src/main/ets/infrastructure/ai/LocalSuggestionProvider.ets
entry/src/main/ets/infrastructure/ai/CloudAiProvider.ets
entry/src/main/ets/infrastructure/speech/SpeechRecognitionProvider.ets
entry/src/main/ets/infrastructure/speech/CoreSpeechRecognitionProvider.ets
entry/src/main/ets/infrastructure/security/InputMethodSecurityModeProvider.ets
```

Provider 必须可替换，以便单元测试使用完全确定性的 Fake Provider，并注入成功、超时、乱序、空结果和协议错误。

### 10.3 UI 层

建议新增：

```text
entry/src/main/ets/presentation/ai/AiActionMenu.ets
entry/src/main/ets/presentation/ai/AiSuggestionPanel.ets
entry/src/main/ets/presentation/voice/VoiceInputBar.ets
entry/src/main/ets/presentation/voice/VoicePermissionGuide.ets
```

复用并扩展：

- `CandidateBar.ets`：增加普通候选、本地关联和 AI 建议三种明确表面。
- `KeyboardRootStage3.ets`：增加 AI/麦克风入口和状态切换。
- `InputSessionStore.ets`：保存当前 AI/语音展示状态，不保存长期内容。
- `EditorContext.ets`：增加 AI 与语音是否允许的派生策略，不把云端设置硬编码进编辑器模型。
- `ImeConnectionService.ets`：增加有界上下文快照和安全替换所需的最小接口。
- `CommitHistoryManager.ets`、`UndoCommitManager.ets`：复用上屏所有权与撤销能力。

### 10.4 Rust 层

第一版不新增生成式模型到 `ime-engine`。建议：

- 扩展 `context-reranker` 或新增独立 `association-predictor` crate。
- 只为已经召回的有限候选增加上下文分，不生成无限文本。
- 为关联词建立冻结数据集、确定性结果和独立性能预算。
- 不让 AI Provider 修改系统词库、用户词库或候选稳定 ID。

---

## 11. 核心协议建议

### 11.1 AI 请求

```text
AiRequest
  -> requestId
  -> sessionGeneration
  -> action
  -> sourceText
  -> sourceOwnership
  -> locale
  -> providerPolicy
  -> deadlineMs
```

`sourceOwnership` 用于区分：

- 当前组合文本。
- 最近一次由输入法上屏的文本。
- 未来用户明确选择的文本。

第一版只允许前两种，来源不明时拒绝自动替换。

### 11.2 AI 建议

```text
AiSuggestion
  -> suggestionId
  -> requestId
  -> text
  -> label
  -> providerKind: LOCAL | CLOUD
  -> replacePolicy: REPLACE_VERIFIED_SOURCE | INSERT_ONLY
```

### 11.3 语音结果

```text
VoiceRecognitionResult
  -> voiceSessionId
  -> sessionGeneration
  -> text
  -> isFinal
  -> providerKind
  -> language
  -> errorCode
```

所有协议字段都必须有长度上限，UI 不得直接展示服务端任意错误详情。

---

## 12. 设置与迁移

当前设置 schema 为 8。第一阶段建议升级到新 schema，并进行显式迁移。建议字段包括：

```text
aiEnabled
localAssociationEnabled
cloudAiEnabled
cloudAiConsentVersion
preferredAiActions
voiceInputEnabled
voiceLanguage
voiceAutoCommit
voiceCloudConsentVersion
voicePunctuationEnabled
```

迁移规则：

- 老用户升级后普通输入行为不变。
- 本地关联可根据产品决策默认关闭或小范围灰度，生成式云 AI 默认关闭。
- 语音输入默认可见但未授权，不在后台请求麦克风。
- 云端同意版本变化后必须重新确认。
- 重置设置应清除 AI/语音开关和同意版本，但不伪装撤销系统权限。
- 清空用户模型不应自动删除设置；删除 AI 个性化数据应有独立入口。

---

## 13. 安全与隐私设计

### 13.1 编辑器策略

| 编辑器类型 | 本地候选 | 云端 AI | 语音 |
| --- | --- | --- | --- |
| 普通文本 | 允许 | 用户开启后允许 | 完整体验模式下允许 |
| 多行文本 | 允许 | 用户开启后允许 | 完整体验模式下允许 |
| 搜索 | 允许 | 第一版仅续写/改写可选 | 完整体验模式下允许 |
| Email/URL | 英文候选允许 | 第一版禁用 | 第一版禁用 |
| 数字/电话 | 基础键盘 | 禁用 | 第一版禁用 |
| 密码/验证码/锁屏 | 禁用学习 | 禁用 | 禁用 |
| 未知类型 | 本地普通输入 | 默认禁用 | 默认禁用 |

### 13.2 数据处理

- 原始音频只在内存中流转，完成、取消或错误后立即释放。
- AI 请求正文不得写入普通日志、崩溃日志或本地分析文件。
- 不保存完整 AI 请求历史；如需历史功能，必须另立设计并由用户开启。
- 服务端默认不使用请求内容训练模型，并提供明确保存期限或无留存策略。
- 网络传输必须使用 TLS；客户端不接受无效证书或明文回退。
- 本地模型、配置和提示模板必须校验版本与完整性。
- 服务端返回只允许文本候选，不接受脚本、动作或富文本执行协议。

### 13.3 日志白名单

允许记录：

- `action=POLITE` 等固定动作 ID。
- `provider=LOCAL/CLOUD`。
- 输入长度桶、候选数量、耗时、是否超时。
- 固定错误码和安全模式。

禁止记录：

- AI 输入和输出正文。
- 语音 PCM、转写正文和语音文件路径。
- 完整编辑器上下文。
- 用户词库正文、联系人和应用标识。
- 可重建个人输入行为的逐事件长期日志。

---

## 14. 性能与资源预算

以下为拟议产品目标，需要通过真机基线调整：

### 14.1 普通输入

- AI/语音关闭时，普通候选质量不得回退。
- AI/语音模块未使用时不得加载大型模型或语音引擎。
- 新增能力对逐键 P95 的影响目标不超过现有基线的 5%。
- 任何网络请求不得占用输入动作串行队列。

### 14.2 本地关联

- 单次重排或联想 P95 目标不超过 10 ms。
- 候选池、查询次数和上下文窗口保持有界。
- 第一版本地增量模型文件建议控制在数 MB 级，额外常驻内存目标不超过 10 MB。

### 14.3 云端 AI

- 网络可用时首批建议 P50 目标不超过 1.5 秒，P95 目标不超过 4 秒。
- 客户端硬超时建议为 8 秒。
- 超时后立即恢复可操作状态，迟到结果必须丢弃。
- 单次输入长度、输出长度和候选数量设置硬上限，控制延迟与费用。

### 14.4 语音

- 点击麦克风后应在 500 ms 内显示明确的准备或录音状态。
- 中间识别结果目标在开始说话后 1 秒内出现；以目标真机实测为准。
- 停止说话后最终结果目标在 1.5 秒内返回；在线模式需按网络分层报告。
- 音频写入、识别回调和 UI 更新不得阻塞普通键盘主线程。
- 取消后应立即停止 UI 录音状态，并异步完成资源释放。

---

## 15. 质量指标

AI 输入法不应只看生成文本“看起来是否聪明”，应衡量实际输入效率。

### 15.1 AI 指标

- AI 入口触发率。
- 建议展示成功率。
- 建议接受率。
- 接受后 5 秒内撤销率。
- 每次接受节省的按键数和字符数。
- 不同动作的接受率：续写、简洁、礼貌、正式、翻译。
- 本地与云端的延迟、超时率和失败回退率。

### 15.2 语音指标

- 麦克风入口触发率。
- 授权成功率和拒绝率。
- 识别启动成功率。
- 首个中间结果延迟和最终结果延迟。
- 语音结果确认率、重说率和取消率。
- 上屏后短时间删除/撤销率。
- 冻结语音集上的字错误率，并按安静、一般噪声、明显噪声和口音分层。

### 15.3 隐私保护指标

正式版本只允许上传经过用户同意的聚合指标。聚合数据不得包含正文、音频、逐条时间线、应用标识或可还原用户身份的字段。第一版可以只做本地验收数据，不急于上线远程埋点。

---

## 16. 测试与验收

### 16.1 AI 单元测试

- Provider 成功、空结果、超长结果、非法 JSON 和协议版本不兼容。
- 超时、取消、乱序返回和旧 generation 返回。
- 用户继续输入、移动光标、隐藏键盘或切换应用后拒绝旧结果。
- 替换前文本匹配、文本不匹配和光标不可读。
- emoji、代理对、组合字符、换行和成对符号安全。
- 密码、未知编辑器和全局 AI 关闭策略。
- 服务端内容包含动作字符串时仍只作为普通文本。

### 16.2 语音单元测试

- 权限已授予、拒绝、撤回和系统限制。
- 基础模式与完整体验模式。
- 引擎创建失败、录音失败、无声音、VAD 超时和识别错误。
- 中间结果乱序、最终结果重复和取消后迟到回调。
- 键盘隐藏、输入停止、Ability 重建、屏幕旋转和应用切换。
- 音频中断、电话/其他录音占用和低内存恢复。
- 上屏失败后结果保留，不显示虚假成功。

### 16.3 真机矩阵

语音识别不支持模拟器，因此至少需要：

- ARM64 手机竖屏/横屏。
- ARM64 平板竖屏/横屏。
- 2in1 设备及实体键盘并存场景。
- 普通文本、搜索、多行和真实第三方应用。
- 安静室内、一般环境噪声和外放干扰。
- 有网、弱网、断网和网络切换。
- 基础安全模式、完整体验模式、麦克风拒绝和权限撤回。

### 16.4 Release 门禁

- 普通版本不得包含永久 API Key、测试地址、用户输入样本或原始录音。
- 若加入网络权限，Release 门禁必须改为按产品配置和白名单验证，不能简单移除检查。
- 麦克风权限、用途说明、隐私政策和实际功能必须一致。
- 端侧模型只允许已登记文件名、大小、哈希和版本。
- Debug Provider、假候选、测试证书和内部日志不得进入正式包。

---

## 17. 分阶段路线

### 17.1 阶段 AI-0：能力与安全底座

目标：不接真实模型，先把架构和门禁做正确。

- 安全模式探测与运行时能力状态。
- AI/语音领域协议。
- Fake AI Provider 和 Fake Speech Provider。
- AI/语音状态机、generation 取消和错误表面。
- 设置迁移和隐私说明骨架。
- 普通输入零回退门禁。

完成标准：使用确定性假数据可以跑通 UI、会话取消、替换、撤销和全部负向场景。

### 17.2 阶段 AI-1：第一版 AI 输入

目标：验证用户是否愿意在输入法中使用生成式改写。

- 本地关联词。
- AI 入口和五个基础动作。
- 自有服务端代理与 CloudAiProvider。
- 最近一次上屏文本的安全替换。
- 超时、离线和基础模式回退。
- 真实第三方编辑器验收。

完成标准：云端不可用时普通输入无影响；用户明确操作后可以获得、选择和撤销 AI 建议。

### 17.3 阶段 VOICE-1：第一版语音输入

目标：提供可靠、可取消、可确认的短语音输入。

- Core Speech Kit 真机能力探针。
- 麦克风授权和完整体验模式引导。
- VoiceInputBar、实时中间结果和最终确认。
- 默认 20 秒短语音。
- 资源释放、错误回退和真机矩阵。

完成标准：手机和平板真机核心流程通过；取消、拒权、断网和会话切换不写入错误文本、不残留录音状态。

### 17.4 阶段 AI-2：体验和成本优化

- 对高接受率动作优化提示与候选格式。
- 增加缓存无正文的能力结果和服务端模型路由，不缓存用户原文。
- 按文本长度和动作设置费用上限。
- 关联词与云端续写协调，避免重复候选。
- 支持选区或上一句，但必须先完成跨宿主文本所有权协议。
- 根据用户反馈调整 AI 入口位置和默认动作。

### 17.5 阶段 VOICE-2：语音增强

- 文本预览受支持编辑器中的实时组合展示。
- 标点、分段和口语词清理。
- 领域热词与用户词库协同。
- 更多语言和方言能力探测。
- 长语音转写作为主应用独立功能，不直接塞入键盘会话。
- 可选 AI 去口语化，但必须二次触发并显示原结果。

### 17.6 阶段 AI-3：端侧模型

- 使用 MindSpore Lite/NNRT 对分类、短预测或轻量改写做 POC。
- 建立不同设备档位的包体、内存、延迟和功耗基线。
- 高频、短文本、隐私敏感能力优先迁移到本地。
- 云端仍用于复杂长文本，并保持用户可关闭。
- 端侧模型未加载或不兼容时自动回退，不影响普通输入。

### 17.7 阶段 AI-4：个性化

- 本地表达偏好和术语偏好。
- AI 建议接受/拒绝的有界学习。
- 个性化数据独立开关、查看、清除和导出。
- 不以长期保存完整输入正文作为个性化前提。

---

## 18. 主要风险与应对

| 风险 | 后果 | 应对 |
| --- | --- | --- |
| 云端延迟或断网 | AI 候选长时间加载 | 硬超时、取消、状态恢复、本地回退 |
| 输入法基础模式限制 | 云端和麦克风不可用 | 运行时探测、明确降级、普通输入独立 |
| 麦克风授权阻力 | 用户无法使用语音 | 主应用解释、按需请求、拒绝后不骚扰 |
| 语音能力设备差异 | 部分机型创建引擎失败 | 真机能力探针、支持矩阵、Provider 降级 |
| AI 覆盖用户新输入 | 文本丢失或误改 | generation、光标核验、文本核验、安全撤销 |
| 模型幻觉或不当内容 | 建议不准确 | 只展示候选、不自动发送、输出校验和举报入口 |
| API Key 泄漏 | 费用与安全风险 | 自有服务端代理，不在 HAP 内保存永久密钥 |
| 云端费用失控 | 商业不可持续 | 文本/输出上限、速率限制、配额和动作分级 |
| 端侧模型过大 | 冷启动、内存和功耗恶化 | 懒加载、可选模型、设备分级、硬资源门禁 |
| 隐私政策与实现不一致 | 审核和信任风险 | 数据最小化、云端标识、同意版本和实包审计 |

---

## 19. 商业与发布建议

- 第一版优先验证留存和输入效率，不急于加入广告或复杂收费体系。
- 本地输入、本地关联和基础语音入口应保持清晰可用。
- 云端生成能力可以采用有限免费额度加订阅，但必须先测算单次动作成本。
- 不建议在键盘候选区域展示广告，避免损害输入信任和可用空间。
- 不建议向普通用户暴露“自填大模型永久 Key”；如未来支持高级用户配置，应另立安全存储和责任边界设计。
- 模型供应商应可由服务端切换，客户端产品文案描述能力，不把核心体验绑定到具体模型名称。

---

## 20. 第一版完成定义

只有同时满足以下条件，才可以声称已经完成第一版 AI/语音转型：

- 普通输入功能、候选质量、用户词库和音形确定性语义没有回退。
- AI 与语音关闭、失败、超时、拒权和不可用时，普通输入始终可用。
- 基础模式下没有尝试使用被禁止的网络或麦克风能力。
- 密码和敏感编辑器不出现 AI、语音候选或学习记录。
- AI 建议不会在用户未确认时修改文字。
- 语音原始音频不落盘，取消和会话切换后不残留录音。
- 云端请求经过用户主动动作和明确同意，只发送有界的必要文本。
- 永久 API Key、测试端点、原始音频和输入样本未进入 Release HAP。
- AI 主机自动化、语音 Fake Provider 自动化和 ARM64 真机语音矩阵均有真实结果。
- 项目状态、隐私设计、权限说明、测试计划和发布门禁同步更新。

---

## 21. 参考资料

### 21.1 项目内资料

- `README.md`
- `PROJECT_STATE.md`
- `docs/ARCHITECTURE.md`
- `docs/architecture/PRIVACY_DESIGN.md`
- `docs/product/planning/PROJECT_PLAN.md`
- `docs/product/requirements/XIAOHE_YINXING_IMPROVEMENTS.md`
- `entry/src/main/ets/domain/editor/EditorContext.ets`
- `entry/src/main/ets/infrastructure/ime/ImeConnectionService.ets`
- `entry/src/main/ets/application/CommitHistoryManager.ets`
- `entry/src/main/ets/application/UndoCommitManager.ets`
- `engine-rust/crates/context-reranker/`

### 21.2 HarmonyOS 官方资料

- Core Speech Kit：<https://developer.huawei.com/consumer/cn/sdk/core-speech-kit>
- 语音识别开发指南：<https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V13/speechrecognizer-guide-V13>
- 输入法安全模式：<https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/ime-kit-security-V5>
- HarmonyOS AI 能力目录：<https://developer.huawei.com/consumer/cn/doc/>
- MindSpore Lite：<https://www.mindspore.cn/lite>
