# 26 键全拼和 9 键拼音加入计划书

## 1. 文档目的

本文档用于指导双羽输入法在现有 17 键双拼、26 键双拼基础上，加入传统 26 键全拼和传统 9 键拼音。

计划遵循以下原则：

- 保留现有 17 键双拼、26 键双拼和小鹤音形能力，不改变既有输入语义。
- 先完成 26 键全拼，再以全拼能力为基础加入 9 键拼音。
- ArkTS 负责键盘界面、交互和 HarmonyOS 能力；C++ 只负责跨语言转发；拼音解析、消歧和候选逻辑放在 Rust。
- 复用现有拼音词库、候选查询、候选排序、整句解码、分页和用户学习能力。
- 阶段数量保持精简，每个阶段都应能够独立实现、测试和验收，避免单个阶段过长。

---

## 2. 建设目标

完成后，中文输入入口应提供以下四种常用键盘方案：

| 键盘方案 | 输入解析方式 | 触屏布局 |
| --- | --- | --- |
| 17 键双拼 | 小鹤双拼 | 17 键分组字母布局 |
| 26 键双拼 | 小鹤双拼 | 标准 QWERTY 布局 |
| 26 键全拼 | 全拼 | 标准 QWERTY 布局 |
| 9 键拼音 | T9 拼音 | 传统 3×4 九宫格布局 |

新增能力应满足：

- 26 键全拼支持连续拼音、未完成音节、明确分音、候选选择、整句转换和删除恢复。
- 9 键拼音支持数字序列到拼音的消歧、拼音组合选择、汉字候选、整句转换和逐键删除。
- 键盘方案可在空闲工具栏中切换并持久化，下次启动恢复用户选择。
- 方案切换、会话切换、横竖屏变化和异常恢复后，界面方案与引擎方案保持一致。
- 新方案继续保持纯本地运行，不增加网络权限或在线依赖。

### 2.1 本计划暂不包含

- 模糊音和地区口音纠错。
- 云候选、云同步和在线语言模型。
- 滑行输入、语音输入和手写输入。
- 声调输入。
- 9 键英文 T9 联想或九键英文多击输入。
- 对现有正式词库进行无关的大规模重建。

9 键方案切换到英文状态时，首版直接使用现有 26 键 QWERTY 英文键盘。实体键盘输入中文时按全拼字母处理，不要求用户用实体数字键模拟 9 键。

---

## 3. 总体设计

### 3.1 分离输入方案与触屏布局

当前“输入方案”和“17/26 键布局”分别保存。新增全拼和 9 键后，需要明确区分：

- **输入方案**：小鹤双拼、全拼、T9 拼音、小鹤音形等编码和候选语义。
- **触屏布局**：17 键分组、26 键 QWERTY、9 键九宫格等按键外形。
- **键盘档案**：面向用户展示的合法组合，例如“26 键全拼”。

建议由统一的键盘档案描述产品入口，内部仍保留输入方案和布局两个维度。非法组合不得进入运行时。

```text
KeyboardProfile
  -> profileId
  -> displayName
  -> inputScheme
  -> layoutKind
  -> engineSchemeId
  -> supportedCapabilities
```

建议首版档案 ID 保持稳定：

```text
xiaohe-17
xiaohe-26
quanpin-26
pinyin-9
```

小鹤音形继续使用自己的引擎方案和既有布局规则，不因新增拼音方案改变候选语义。

### 3.2 通用拼音解析边界

Rust 引擎增加统一的拼音解析边界，具体解析方式由后端实现：

```text
PhoneticParser
  ├─ XiaoheShuangpinParser
  ├─ QuanpinParser
  └─ T9PinyinParser
```

三种解析器统一输出：

- 原始按键输入。
- 标准化拼音音节或有界音节路径。
- 未完成音节。
- 明确分音位置。
- 用于候选查询的查询意图。
- 候选栏应显示的输入分段。
- 当前首选拼音以及可选拼音组合。

ArkTS 和 C++ 不切分拼音、不展开 9 键字母组合、不排序拼音路径。

### 3.3 可复用与新增范围

```text
26 键全拼字母 / 9 键数字
  -> Rust 对应拼音解析器
  -> 标准化拼音音节或音节图
  -> 现有拼音词库
  -> 现有候选查询 / 整句解码
  -> 现有候选排序 / 用户学习
  -> 现有候选会话 / 分页
  -> ArkTS 候选栏与文本提交
```

原则上复用：

- 标准 QWERTY 中文布局。
- 拼音音节清单和正式拼音词库。
- 候选查询、排序、整句解码、分页和提交边界。
- 用户学习的存储、恢复和清理机制。
- 空闲工具栏、键盘选择面板、候选栏和设置入口。

需要新增或调整：

- 通用键盘档案及旧偏好迁移。
- 全拼解析器。
- T9 拼音解析器和数字音节索引。
- 9 键布局和拼音组合选择界面。
- 解析器提供的显示分段和拼音组合协议。
- 新方案对应的自动化测试、性能基线和真机验收。

### 3.4 方案切换规则

- 只允许在没有活动组合输入时从空闲工具栏切换方案。
- 切换时先请求 Rust 引擎切换并取得成功结果，再更新 ArkTS 状态和持久化配置。
- 任一步失败都回滚到原方案，不允许界面布局与引擎方案分叉。
- 切换完成后清空旧候选、旧拼音组合和旧分页状态。
- 密码、数字、电话等受限输入框继续优先遵循现有编辑器策略。

### 3.5 用户数据规则

- 用户学习继续按稳定方案 ID 隔离，避免双拼、全拼和 9 键的学习记录相互污染。
- 现有双拼人工词库编码不得被全拼或 9 键直接解释。
- 人工用户词库应增加方案命名空间，或为新增方案使用独立规则文件。
- 9 键候选查询使用标准化拼音作为语言信息；原始数字串只用于 T9 解析和界面展示。

---

## 4. 分阶段实施计划

本计划分为五个阶段。第一阶段建立公共边界，第二阶段交付 26 键全拼，第三、四阶段完成 9 键核心和界面，第五阶段进行整体收口。每个阶段完成验收后再进入下一阶段。

## 阶段一：键盘档案与通用协议

> 完成状态（2026-08-10）：`COMPLETED（主机自动化与 Release 构建范围）`。
> 已完成统一 `KeyboardProfile`、旧 17/26 键及音形配置迁移、Rust `PhoneticParser` 与小鹤适配、
> 解析器负责的展示分段，以及 ABI/interface 6 的 `currentPinyin` / `pinyinCombinations` 协议。
> 阶段一交付时 `quanpin-26` 与 `pinyin-9` 仅登记且保持禁用；当前状态以后续阶段记录为准。实现与验收记录见
> [`docs/PINYIN_STAGE1_KEYBOARD_PROFILE_AND_PROTOCOL.md`](docs/PINYIN_STAGE1_KEYBOARD_PROFILE_AND_PROTOCOL.md)。

### 阶段目标

在不改变现有双拼行为的前提下，建立全拼和 9 键都能复用的方案、解析结果和跨层协议。

### 实现范围

- 建立通用键盘档案模型，替代仅表达“17/26 键双拼”的布局概念。
- 将当前独立保存的 `schemeId` 与键盘布局偏好纳入统一、可校验的配置流程。
- 增加旧配置迁移：原 17 键映射到 `xiaohe-17`，原 26 键映射到 `xiaohe-26`，未知值安全回退到 `xiaohe-26`。
- 在 Rust 中抽象通用拼音解析结果，先用适配方式接入现有小鹤双拼解析器。
- 将输入分段改为解析器负责，取消协议层默认按两个字母切段的假设。
- 为后续 9 键预留“当前拼音”和“可选拼音组合”字段；普通双拼和全拼没有多选时返回空列表。
- 必要时升级 interface/ABI，并同步 ArkTS、C++、Rust 类型定义和兼容性检查。

### 阶段交付物

- 通用键盘档案与配置迁移。
- 通用拼音解析结果和跨层协议。
- 现有小鹤双拼适配实现。
- 更新后的架构说明、协议说明和回归测试。

### 验收标准

- 17 键双拼、26 键双拼和小鹤音形行为与修改前一致。
- 旧安装配置升级后仍恢复原键盘选择。
- 组合输入、删除、候选提交、分页和方案切换回归通过。
- ArkTS 和 C++ 中没有新增拼音切分或候选排序逻辑。

---

## 阶段二：26 键全拼完整链路

> 完成状态（2026-08-10）：`COMPLETED（主机自动化与 Release 构建范围）`。
> 已交付 Rust 全拼 Trie 与有界动态规划解析、完整/未完成音节、显式分音和歧义路径，
> 并接通精确/前缀查询、整句、分页、提交、用户学习、`qp` 人工词库隔离及统一档案切换。
> `quanpin-26` 已正式启用，`pinyin-9` 继续禁用；模拟器和 ARM64 真机专项为 `NOT_RUN`。
> 后续实体键盘性能修订已将完整引擎平均延迟由 `82.359 ms/键` 降至 `2.027 ms/键`，
> 2026-08-11 质量修订已取消 24 字母自动收口；全拼只允许明确用户动作上屏，并加入 `n` 完整音节/前缀混合召回、词库上下文排序和真实句质量基线。
> 实现、性能基线和验收记录见
> [`docs/PINYIN_STAGE2_QUANPIN_26.md`](docs/PINYIN_STAGE2_QUANPIN_26.md)。

### 阶段目标

交付可正式使用的 26 键全拼，形成 9 键拼音后续复用的标准拼音解析和候选基线。

### 实现范围

- 新增 Rust 全拼解析器，基于现有合法拼音音节建立 Trie。
- 使用动态规划或有界音节图解析连续字母，不使用简单贪心切分。
- 支持完整音节、末尾未完成音节和 `'` 明确分音。
- 对 `xian`、`xi'an` 等存在多种切分可能的输入保留有界路径，由词库和整句解码结果参与排序。
- 将标准化音节接入现有精确查询、前缀查询、整句解码、分页和用户学习。
- 复用现有 QWERTY 中文布局，在空闲工具栏和设置入口加入“26 键全拼”。
- 在候选栏显示解析器提供的全拼分段，不再按双拼两码格式展示。
- 明确全拼用户学习和人工用户词库的方案隔离。

### 阶段交付物

- 全拼解析器及单元测试。
- `quanpin-26` 引擎和键盘档案。
- 26 键全拼候选、整句、提交、删除、分音和切换链路。
- 全拼正确性及性能基线报告。

### 验收标准

- `ni`、`nihao`、`zhongguo` 等常用输入能够产生正确候选。
- `zhon` 等未完成拼音能够提供合理前缀候选。
- `xian` 与 `xi'an` 按各自语义解析，删除任意字母后能够稳定恢复上一状态。
- 空格、回车、候选点击、部分提交和候选分页遵循现有输入法规则。
- 从双拼切到全拼或从全拼切回双拼后，布局、引擎、工具栏名称和持久化配置一致。
- 现有双拼、音形和编辑器兼容性测试全部通过。

---

## 阶段三：9 键拼音引擎与消歧

> 2026-08-12 状态：`IMPLEMENTED / HOST_VALIDATION_PARTIAL`。Rust 引擎、消歧、候选复用和 ABI 7 已实现；`64` 与 `64426` 专项、ArkTS、双 ABI Native、Release HAP 和内容门禁通过。由于既有小鹤冻结候选基线与当前生产词库仍有 1 项排序不一致，本阶段不标记 `COMPLETED`。九宫格 UI 未提前实现，`pinyin-9` 档案保持禁用。详见 `docs/PINYIN_STAGE3_T9_ENGINE.md` 与 ADR 0025。

### 阶段目标

在 Rust 内完成传统 9 键数字序列到拼音及汉字候选的核心能力，不在 UI 层猜测字母。

### 实现范围

- 使用标准 T9 映射：`2 ABC`、`3 DEF`、`4 GHI`、`5 JKL`、`6 MNO`、`7 PQRS`、`8 TUV`、`9 WXYZ`。
- 离线或初始化时将合法拼音音节转换为数字签名，建立数字音节 Trie。
- 输入数字时直接查询合法拼音路径，禁止按 `3^n` 或 `4^n` 展开所有字母组合。
- 将数字序列解析为有界的拼音音节路径，并复用全拼阶段的词库查询和整句解码能力。
- 增加当前首选拼音、可选拼音组合和切换拼音组合的引擎接口。
- 支持逐个删除数字、清空组合、明确分音、候选提交和分页。
- 对路径数量、搜索深度、候选数量和缓存容量设置统一上限，建立最坏输入性能测试。
- 用户学习使用 `pinyin-9` 方案命名空间，学习对象是最终候选和标准拼音，不直接学习不稳定的临时展开路径。

### 阶段交付物

- T9 数字音节索引和 9 键拼音解析器。
- 拼音组合查询、选择、删除和恢复接口。
- 9 键候选查询、整句解码和学习链路。
- 数字碰撞用例、长输入用例和性能基线。

### 验收标准

- `64` 能产生包含 `ni` 在内的合法拼音组合和对应汉字候选。
- `64426` 能解析出 `ni'hao` 路径并提供“你好”等合理候选。
- 同一数字串对应多个合法拼音时，能够稳定展示、切换和记忆当前选择。
- 删除一个数字后，拼音组合和汉字候选同步回到对应状态。
- 连续长数字输入不会出现组合爆炸、界面长时间卡顿或无界内存增长。
- ArkTS 和 C++ 只传递数字及选择操作，不包含 T9 消歧算法。

---

## 阶段四：传统 9 键界面与交互

### 阶段目标

将 9 键引擎接入正式软键盘，完成用户可操作、可理解、可切换的传统九宫格体验。

### 实现范围

- 新增 `pinyin-9` 键盘档案和传统 3×4 九宫格中文布局。
- 数字键主文案显示数字，次文案显示对应字母组。
- 建议基础排列如下：

| 第一行 | 第二行 | 第三行 | 第四行 |
| --- | --- | --- | --- |
| `1 标点`、`2 ABC`、`3 DEF` | `4 GHI`、`5 JKL`、`6 MNO` | `7 PQRS`、`8 TUV`、`9 WXYZ` | `符/123`、`0 分词`、`删除` |

- 底部继续保留中/英、空格和动态动作键，具体宽度按现有键盘高度与横竖屏策略适配。
- 9 键中文数字必须发送专用拼音数字动作，不能走普通数字直输路径。
- 输入状态下展示当前首选拼音；存在多个拼音组合时提供独立的拼音组合选择区域。
- `1` 在中文 9 键空闲或组合允许的状态下打开常用标点；`0` 在组合中插入明确分音边界。直接数字输入继续通过“符/123”进入数字键盘。
- 空格在有候选时提交首选候选，无候选时输入普通空格；删除优先删除组合数字，组合为空时删除编辑器文本。
- 中英文切换到英文时使用现有 QWERTY 英文布局，切回中文后恢复 9 键布局。
- 补齐按压反馈、键帽预览、无障碍文案、深浅色主题、横竖屏和手机/平板适配。

### 阶段交付物

- 正式 9 键拼音布局和按键动作。
- 拼音组合展示与选择组件。
- 空闲工具栏、键盘选择面板和设置中的“9 键拼音”入口。
- 9 键界面自动化测试及设备截图证据。

### 验收标准

- 九宫格键位、主次文案、删除、分音、标点和底部功能键行为符合设计。
- 数字键不会把数字直接提交到中文编辑框，也不会被错误当作普通字符。
- 拼音组合切换后，汉字候选立即同步更新。
- 键盘隐藏、应用切换、编辑框切换和横竖屏变化后不会残留旧数字串或旧拼音组合。
- 手机和平板、深色和浅色主题下无按键重叠、截断或点击区域错位。
- 无障碍服务能够读出数字、对应字母组和功能键含义。

---

## 阶段五：联合验收与发布收口

### 阶段目标

完成四种键盘方案的联合回归、数据迁移、性能验证和发布门禁，使新增方案达到与现有方案一致的稳定性标准。

### 实现范围

- 覆盖四种键盘档案的安装、升级、切换、重启恢复和异常回滚。
- 验证普通文本、搜索、聊天、多行、邮箱、URL、密码、数字和电话输入框策略。
- 验证候选点击、空格提交、回车提交、部分提交、分页、连续删除和组合取消。
- 验证全拼歧义切分、9 键数字碰撞、长组合输入和无候选状态。
- 验证用户学习、清空学习、人工用户词库隔离和损坏数据恢复。
- 以现有性能门禁为基准冻结新增方案指标；新增方案不得破坏现有双拼热路径。
- 完成 ArkTS 单元测试、Rust fmt/clippy/test、跨层协议测试、Release HAP 构建和包内容检查。
- 在 Phone、Pad、横竖屏和至少一个真实第三方输入场景完成设备验收。

### 阶段交付物

- 联合自动化测试和设备验收报告。
- 26 键全拼与 9 键拼音性能基线。
- 配置迁移、回滚和兼容性报告。
- 更新后的 README、架构文档、协议文档、CHANGELOG 和项目状态文档。

### 验收标准

- 四种键盘方案均可选择、输入、提交、删除、切换并在重启后恢复。
- 旧版本升级不丢失用户原有方案、学习数据和人工词库。
- 新增方案失败时能够安全回退，不影响现有双拼和音形输入。
- 自动化测试、静态检查、Release 构建和资源门禁全部通过。
- 性能测试不存在无界搜索、明显输入延迟或持续内存增长。
- 发布包不包含测试词库、调试入口、基准临时文件或原始构建数据。

---

## 5. 关键测试矩阵

| 测试类别 | 26 键全拼重点 | 9 键拼音重点 |
| --- | --- | --- |
| 基础输入 | 单音节、词语、整句 | 单数字、多数字、词语、整句 |
| 未完成输入 | 音节前缀召回 | 数字前缀对应的拼音召回 |
| 歧义处理 | `xian` 与 `xi'an` | 同一数字串的多个拼音组合 |
| 删除恢复 | 逐字母删除和分音删除 | 逐数字删除和分音删除 |
| 候选操作 | 点击、空格、回车、分页、部分提交 | 拼音组合选择后再执行候选操作 |
| 方案切换 | 双拼与全拼互切 | 9 键与其他方案互切 |
| 用户数据 | 全拼学习和词库隔离 | 标准拼音学习与数字输入隔离 |
| 生命周期 | 编辑框、应用、键盘显示隐藏 | 同时清理数字串和拼音组合 |
| 性能 | 长全拼和多切分路径 | 长数字串和高碰撞数字串 |

---

## 6. 主要风险与控制措施

### 6.1 全拼切分歧义

风险：连续全拼存在多种合法音节切分，使用贪心算法会产生错误候选。

控制：使用音节 Trie 和有界动态规划；明确分音符作为硬边界；由词库和整句解码参与路径排序。

### 6.2 9 键组合爆炸

风险：逐位展开 ABC 字母组合会随输入长度指数增长。

控制：预先建立合法拼音数字签名索引，只搜索合法音节路径；统一限制路径数、搜索深度和候选快照大小。

### 6.3 布局与引擎状态不一致

风险：布局偏好保存成功但引擎切换失败，导致按键语义与候选解析不一致。

控制：使用统一键盘档案和串行切换事务；引擎成功后才更新界面和持久化；失败时完整回滚。

### 6.4 用户数据串用

风险：双拼原始码、全拼字母和 9 键数字被同一人工规则错误解释。

控制：学习键和人工词库均按方案命名空间隔离；候选排序只使用标准化且来源明确的数据。

### 6.5 跨层协议继续依赖双拼假设

风险：现有两码分段、只接受字母按键等假设使全拼或 9 键在 ArkTS/C++ 中出现补丁逻辑。

控制：第一阶段先完成通用协议；显示分段、拼音组合和查询意图均由 Rust 返回，C++ 仅转发，ArkTS 仅展示。

---

## 7. 阶段依赖与完成顺序

```text
阶段一：键盘档案与通用协议
        ↓
阶段二：26 键全拼完整链路
        ↓
阶段三：9 键拼音引擎与消歧
        ↓
阶段四：传统 9 键界面与交互
        ↓
阶段五：联合验收与发布收口
```

不得绕过阶段二直接在 ArkTS 中实现 9 键字母展开。9 键必须复用已经验收的标准拼音、候选和整句能力，避免形成第二套词库查询与候选排序体系。

---

## 8. 最终完成定义

只有同时满足以下条件，才能认为“26 键全拼和 9 键拼音”整体完成：

- 四种正式键盘方案均可从统一入口选择并正确持久化。
- 26 键全拼和 9 键拼音均使用正式本地拼音词库产生候选。
- 全拼切分和 T9 消歧位于 Rust，ArkTS/C++ 没有复制业务算法。
- 候选查询、排序、整句、分页、学习和提交继续共用既有正式链路。
- 旧用户配置和数据完成安全迁移，现有双拼与音形行为无回归。
- 自动化、性能、设备和 Release 构建验收全部完成并留下可复核证据。
