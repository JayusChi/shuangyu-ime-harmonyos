----syntax=cn, code
----leadkey=;
----config=ime-kf.ini
-- 每条为“上屏文字<TAB>编码”，中间必须使用一个 Tab，不能用空格代替。
-- _ 是单击引导键时显示的默认候选；; 是再次按引导键时直接上屏的内容。
-- 其他编码使用 1～4 位 a-z 小写字母；同码多条会按文件顺序显示为候选。
：	_
；	;
：“	q
？	w
（	e
）	r
《	y
》	u
！	a
……	s
、	d
·	g
“	z
→	x
”	c
——	v
@	m
