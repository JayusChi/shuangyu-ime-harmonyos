# 纯血鸿蒙双拼输入法项目开发计划书

## 一、项目基本信息

### 1.1 项目名称

暂定名称：**鸿蒙双拼输入法**

英文工程名建议：

```text
HarmonyShuangpinIME
```

Bundle Name 示例：

```text
com.example.harmonyshuangpinime
```

正式发布前再更换为真实域名和应用包名。

### 1.2 项目目标

开发一款运行于纯血鸿蒙系统的本地双拼输入法，支持：

- 双拼编码输入；
- 中文候选词展示；
- 候选词选择与翻页；
- 连续输入和整句转换；
- 中文、英文、数字、符号模式切换；
- 输入框类型适配；
- 本地用户词频学习；
- 双拼方案切换；
- 键盘主题、震动和按键音设置；
- 完整的输入法生命周期管理；
- 稳定的 ArkTS、C++、Rust 跨语言调用。

项目采用严格分层：

```text
ArkTS 鸿蒙应用层
        ↓
C++ Native 桥接层
        ↓
Rust 输入法核心引擎
```

### 1.2.1 客户修改验收增补（2026-07-22）

客户修改阶段 3.8、4.1、4.2 已按最终实现完成 Phone/Pad x86_64 模拟器与自动化验收：

- 3.8 经真实运行验证确定为路线 C：目标系统拒绝第三方输入法创建
  `FLAG_CANDIDATE` 面板；Phone 使用顶部候选栏，Pad `AUTO` 使用输入法窗口内部
  固定右侧候选条，“跟随光标（实验）”在设置中禁用并说明平台原因。
- 4.1 候选区以实线下划线显示精确原始 `rawInput`，不使用引擎格式化后的
  `preeditText` 代替用户逐键编码。
- 4.2 普通候选视觉与无障碍文案只显示 `candidate.text`；`candidate.reading` 继续
  保留在跨层协议中，不改变候选排序、学习和提交。
- 最终 ArkTS 284/284、Rust fmt/clippy/test、Release HAP 构建、Release 资源门禁和
  包内容检查均通过。真实 USB/蓝牙键盘、ARM64 物理真机、第三方浏览器与 Pad 分屏
  仍属于扩展兼容性覆盖，不写成已验收。

当前状态与证据以根目录 `PROJECT_STATE.md` 和 `docs/evidence/` 为准。

### 1.3 项目定位

第一版定位为：

- 纯本地输入法；
- 不依赖服务器；
- 不申请网络权限；
- 手机端优先；
- 双拼输入优先；
- 小鹤双拼作为第一个落地方案；
- 架构上支持后续增加微软双拼、自然码、搜狗双拼等方案；
- 优先完成稳定可用的 MVP，不在第一版加入 AI 联想、语音输入、手写输入和云同步。

------

# 二、总体技术架构

## 2.1 分层架构

```text
┌──────────────────────────────────────────┐
│              ArkTS 鸿蒙层                 │
│                                          │
│ InputMethodExtensionAbility              │
│ IME Kit                                  │
│ 键盘 UI / 候选栏 / 设置页面               │
│ 输入状态管理 / 生命周期 / 输入框适配        │
│ 文本提交 / 删除 / 震动 / 音效 / 主题        │
└────────────────────┬─────────────────────┘
                     │ Node-API
┌────────────────────▼─────────────────────┐
│              C++ 桥接层                   │
│                                          │
│ ArkTS 参数校验与类型转换                   │
│ Native 模块注册                           │
│ Rust C ABI 调用                           │
│ 返回值转换为 ArkTS 对象                   │
│ 错误码转换 / 内存释放 / 句柄管理            │
└────────────────────┬─────────────────────┘
                     │ C ABI
┌────────────────────▼─────────────────────┐
│              Rust 核心引擎                 │
│                                          │
│ 双拼方案 / 编码解析 / 拼音校验              │
│ 词库索引 / 候选查询 / 候选排序              │
│ 整句解码 / 用户词频 / 引擎状态机            │
└──────────────────────────────────────────┘
```

## 2.2 依赖方向

依赖方向必须固定为：

```text
ArkTS → C++ → Rust
```

禁止出现：

```text
Rust → ArkTS
Rust → 鸿蒙系统 API
C++ → ArkUI 页面
C++ → 双拼业务算法
ArkTS → 词库内部索引
ArkTS → 候选排序算法
```

## 2.3 核心工程原则

1. ArkTS 只处理鸿蒙系统能力和界面。
2. Rust 不依赖 HarmonyOS API，尽量保持跨平台可测试。
3. C++ 不实现任何输入法业务算法。
4. UI 组件不能直接调用 Native 模块，必须经过应用服务层。
5. 页面不能直接维护复杂输入法状态。
6. 词库不能以大型文本文件形式在每次启动时重新解析。
7. 所有跨语言接口必须有版本号、错误码和内存释放规则。
8. 所有阶段必须先写接口和测试，再补充具体实现。
9. 不允许创建一个几千行的 `Index.ets`、`engine.rs` 或 `napi_init.cpp`。
10. 一个文件只承担一种主要职责。

------

# 三、各语言职责划分

## 3.1 ArkTS 鸿蒙层职责

ArkTS 负责所有与 HarmonyOS 和用户界面相关的功能。

### 3.1.1 输入法 ExtensionAbility

负责：

- 输入法 ExtensionAbility 创建；
- 输入法窗口初始化；
- 输入法窗口显示和隐藏；
- 输入法会话开始；
- 输入法会话结束；
- 应用切换处理；
- 输入框切换处理；
- 资源释放；
- 异常恢复。

ExtensionAbility 文件只负责生命周期分发，不在其中编写键盘布局、候选查询和复杂状态逻辑。

### 3.1.2 IME Kit 适配

单独封装 `ImeConnectionService`，负责：

- 获取输入法服务对象；
- 插入文本；
- 删除文本；
- 获取光标附近文本；
- 发送回车；
- 隐藏键盘；
- 处理编辑框连接变化；
- 处理系统输入法事件。

其他 ArkTS 模块不得直接散乱调用 IME Kit。

### 3.1.3 键盘 UI

键盘拆分为独立组件：

- 字母按键组件；
- 功能按键组件；
- 空格键组件；
- 删除键组件；
- 回车键组件；
- 模式切换键组件；
- 单行键盘组件；
- 中文键盘布局；
- 英文键盘布局；
- 数字键盘布局；
- 符号键盘布局；
- 按键弹出预览组件。

按键组件只负责展示和发送按键事件，不负责解析双拼。

### 3.1.4 候选栏

候选栏负责：

- 展示当前组合文本；
- 展示候选词；
- 候选词点击；
- 横向滑动；
- 展开候选面板；
- 候选翻页；
- 加载状态；
- 引擎异常状态；
- 无候选状态。

候选栏不得自行排序候选词。

### 3.1.5 设置页面

设置页面建议包含：

- 双拼方案选择；
- 模糊音设置；
- 按键震动开关；
- 按键音开关；
- 键盘高度；
- 候选字体大小；
- 浅色、深色和跟随系统；
- 用户词频开关；
- 清空用户词频；
- 隐私说明；
- 关于页面；
- 调试信息页面，仅 Debug 构建显示。

### 3.1.6 输入框类型识别

根据编辑框属性切换键盘策略：

- 普通文本；
- 密码；
- 数字；
- 电话号码；
- 邮箱；
- URL；
- 搜索；
- 多行文本。

密码输入框必须：

- 禁止用户词频学习；
- 禁止记录输入内容；
- 禁止输出包含原始输入的调试日志；
- 根据系统能力调整候选展示策略。

### 3.1.7 文本提交与删除

文本处理规则：

- 存在未完成的双拼组合时，删除键优先删除组合缓冲区；
- 组合缓冲区为空时，调用 IME Kit 删除编辑框文本；
- 点击候选词时，提交候选词并清空或更新组合状态；
- 点击空格时，中文模式优先提交首选候选；
- 点击回车时，先处理未提交组合，再根据输入框类型发送对应动作；
- 长按删除键时由 ArkTS 控制重复触发节奏。

### 3.1.8 生命周期

必须处理：

- 输入法首次创建；
- 输入会话开始；
- 输入会话结束；
- 键盘显示；
- 键盘隐藏；
- 编辑框切换；
- 前台应用切换；
- 横竖屏切换；
  -系统主题变化；
- 内存压力；
- ExtensionAbility 销毁。

### 3.1.9 震动、音效和主题

ArkTS 负责：

- 按键震动；
- 删除键连续震动节流；
- 按键音；
- 静音模式适配；
- 深色模式；
- 高对比度；
- 键盘高度；
- 横屏布局；
- 安全区域适配。

------

## 3.2 Rust 核心引擎职责

Rust 负责所有输入法业务算法。

### 3.2.1 双拼方案配置

双拼方案不得写死在解析代码中。

建议配置结构：

```json
{
  "id": "xiaohe",
  "name": "小鹤双拼",
  "initials": {
    "v": "zh",
    "i": "ch",
    "u": "sh"
  },
  "finals": {
    "q": "iu",
    "w": "ei",
    "r": "uan"
  },
  "zeroInitialRules": [],
  "specialRules": []
}
```

需要提供：

- 方案加载；
- 配置校验；
- 初声映射；
- 韵母映射；
- 零声母规则；
- 特殊音节规则；
- 方案版本；
- 方案切换。

### 3.2.2 双拼编码解析

负责将按键序列转换成可能的拼音音节。

例如：

```text
输入按键 → 双拼拆分 → 声母韵母组合 → 拼音音节
```

解析器必须支持：

- 单个双拼编码；
- 连续双拼编码；
- 不完整编码；
- 非法编码；
- 零声母；
- 编码回退；
- 分词歧义；
- 多种可能切分。

### 3.2.3 合法拼音音节校验

建立合法拼音音节表或自动机，校验：

- 声母和韵母是否可以组合；
- 音节是否真实存在；
- 零声母是否合法；
- 特殊音节是否匹配；
- 连续编码如何切分。

禁止只使用大量嵌套 `if` 判断。

建议使用：

- Trie；
- DFA；
- 静态哈希表；
- 构建期生成的音节数据。

### 3.2.4 词库索引

词库需要离线构建，不能在输入法启动时解析大型 CSV 或 TXT。

建议流程：

```text
原始词库
   ↓
词库构建工具
   ↓
数据清洗
   ↓
拼音标准化
   ↓
索引生成
   ↓
二进制词库文件
   ↓
输入法运行时加载
```

词库至少包含：

- 单字；
- 常用词；
- 多字词；
- 拼音编码；
- 基础词频；
- 词条 ID；
- 可选词性；
- 可选来源标记。

建议独立开发桌面端词库工具：

```text
tools/lexicon-builder
```

### 3.2.5 候选词查询

输入：

```text
双拼原始编码
当前解析结果
上下文信息
候选数量
分页参数
```

输出：

```text
组合文本
候选词列表
候选读音
候选来源
是否还有下一页
```

候选查询模块不直接关心 ArkTS 页面。

### 3.2.6 候选排序

候选排序可以综合：

- 基础词频；
- 用户选择次数；
- 最近使用时间；
- 当前上下文；
- 单字或词语长度；
- 完整匹配程度；
- 词库优先级；
- 整句解码评分。

排序公式不能散落在查询代码中，应集中在 `ranking` 模块。

### 3.2.7 整句解码

后续版本支持：

```text
双拼编码序列
    ↓
构建音节切分图
    ↓
生成候选词路径
    ↓
计算路径得分
    ↓
Beam Search 或 Viterbi
    ↓
输出整句候选
```

第一版可以先实现：

- 单字查询；
- 词语查询；
- 短句组合。

第二版再实现完整整句解码。

### 3.2.8 用户词频加权

用户选择候选后，Rust 引擎记录：

- 词条 ID；
- 选择次数；
- 最近选择时间；
- 输入上下文；
- 双拼编码；
- 学习权重。

用户模型必须支持：

- 加载；
- 更新；
- 快照；
- 原子保存；
- 数据损坏恢复；
- 清空；
- 禁用；
- 版本迁移。

密码输入场景不得更新用户词频。

------

## 3.3 C++ 桥接层职责

C++ 只负责桥接，不实现业务逻辑。

### 3.3.1 可以做的事情

- 注册 Node-API 模块；
- 接收 ArkTS 参数；
- 检查参数类型；
- 将字符串转换为 UTF-8；
- 调用 Rust C ABI；
- 处理 Rust 返回值；
- 创建 ArkTS 对象和数组；
- 将错误码转换为异常或结果对象；
- 管理 Rust 引擎句柄；
- 管理跨语言内存；
- 处理线程安全；
- 释放 Rust 返回缓冲区。

### 3.3.2 禁止做的事情

C++ 中禁止出现：

- 双拼映射表；
- 拼音合法性规则；
- 候选排序公式；
- 词库查询算法；
- 用户词频算法；
- 整句解码；
- 输入状态机；
- 业务数据持久化。

### 3.3.3 推荐桥接接口

ArkTS 调用接口建议设计为：

```typescript
interface NativeImeEngine {
  createEngine(config: EngineConfig): number
  destroyEngine(handle: number): void

  processKey(handle: number, key: string): CompositionResult
  backspace(handle: number): CompositionResult
  selectCandidate(handle: number, index: number): CompositionResult
  changeScheme(handle: number, schemeId: string): CompositionResult
  reset(handle: number): void

  loadUserModel(handle: number, path: string): void
  flushUserModel(handle: number): void

  getEngineVersion(): string
}
```

Rust C ABI 建议：

```c
typedef void* ImeEngineHandle;

int32_t ime_engine_create(
    const char* config_json,
    ImeEngineHandle* out_handle
);

int32_t ime_engine_process_key(
    ImeEngineHandle handle,
    const char* key_utf8,
    ImeBuffer* out_buffer
);

int32_t ime_engine_backspace(
    ImeEngineHandle handle,
    ImeBuffer* out_buffer
);

int32_t ime_engine_select_candidate(
    ImeEngineHandle handle,
    uint32_t index,
    ImeBuffer* out_buffer
);

void ime_engine_free_buffer(ImeBuffer buffer);

void ime_engine_destroy(ImeEngineHandle handle);
```

第一版建议让 Rust 返回 UTF-8 JSON 缓冲区，由 C++ 解析并转换成 ArkTS 对象。

如果性能测试确认 JSON 转换成为瓶颈，再改成扁平 C 结构体。不要在没有性能数据时提前增加 FFI 复杂度。

------

# 四、推荐工程目录

```text
HarmonyShuangpinIME/
├─ AppScope/
│  ├─ app.json5
│  └─ resources/
│
├─ entry/
│  ├─ src/main/
│  │  ├─ module.json5
│  │  │
│  │  ├─ ets/
│  │  │  ├─ ability/
│  │  │  │  └─ MainAbility.ets
│  │  │  │
│  │  │  ├─ extension/
│  │  │  │  └─ ShuangpinInputMethodAbility.ets
│  │  │  │
│  │  │  ├─ application/
│  │  │  │  ├─ KeyboardController.ets
│  │  │  │  ├─ CandidateController.ets
│  │  │  │  ├─ InputSessionController.ets
│  │  │  │  ├─ SettingsController.ets
│  │  │  │  └─ EngineCoordinator.ets
│  │  │  │
│  │  │  ├─ domain/
│  │  │  │  ├─ model/
│  │  │  │  │  ├─ Candidate.ets
│  │  │  │  │  ├─ CompositionState.ets
│  │  │  │  │  ├─ KeyboardMode.ets
│  │  │  │  │  └─ EditorContext.ets
│  │  │  │  ├─ event/
│  │  │  │  └─ contract/
│  │  │  │
│  │  │  ├─ infrastructure/
│  │  │  │  ├─ ime/
│  │  │  │  │  ├─ ImeConnectionService.ets
│  │  │  │  │  └─ EditorAttributeMapper.ets
│  │  │  │  ├─ native/
│  │  │  │  │  ├─ NativeEngineGateway.ets
│  │  │  │  │  └─ NativeEngineTypes.ets
│  │  │  │  ├─ storage/
│  │  │  │  │  ├─ SettingsRepository.ets
│  │  │  │  │  └─ ResourceInstaller.ets
│  │  │  │  ├─ audio/
│  │  │  │  └─ haptic/
│  │  │  │
│  │  │  ├─ presentation/
│  │  │  │  ├─ keyboard/
│  │  │  │  │  ├─ KeyboardRoot.ets
│  │  │  │  │  ├─ ChineseKeyboard.ets
│  │  │  │  │  ├─ EnglishKeyboard.ets
│  │  │  │  │  ├─ NumberKeyboard.ets
│  │  │  │  │  ├─ SymbolKeyboard.ets
│  │  │  │  │  └─ components/
│  │  │  │  ├─ candidate/
│  │  │  │  │  ├─ CandidateBar.ets
│  │  │  │  │  └─ CandidatePanel.ets
│  │  │  │  └─ settings/
│  │  │  │
│  │  │  ├─ state/
│  │  │  │  ├─ InputSessionStore.ets
│  │  │  │  ├─ KeyboardStore.ets
│  │  │  │  └─ SettingsStore.ets
│  │  │  │
│  │  │  └─ common/
│  │  │     ├─ constants/
│  │  │     ├─ error/
│  │  │     └─ logging/
│  │  │
│  │  ├─ cpp/
│  │  │  ├─ CMakeLists.txt
│  │  │  ├─ napi/
│  │  │  │  ├─ module_init.cpp
│  │  │  │  ├─ engine_napi.cpp
│  │  │  │  └─ napi_converter.cpp
│  │  │  ├─ bridge/
│  │  │  │  ├─ rust_engine_bridge.cpp
│  │  │  │  ├─ rust_engine_bridge.h
│  │  │  │  ├─ engine_handle.cpp
│  │  │  │  └─ engine_handle.h
│  │  │  ├─ error/
│  │  │  └─ include/
│  │  │     └─ ime_engine_ffi.h
│  │  │
│  │  ├─ resources/
│  │  └─ types/
│  │     └─ libime_bridge/
│  │        └─ index.d.ts
│  │
│  └─ src/test/
│
├─ engine-rust/
│  ├─ Cargo.toml
│  ├─ Cargo.lock
│  ├─ crates/
│  │  ├─ ime-engine/
│  │  ├─ ime-state/
│  │  ├─ shuangpin-schema/
│  │  ├─ shuangpin-parser/
│  │  ├─ pinyin-syllable/
│  │  ├─ lexicon-core/
│  │  ├─ candidate-query/
│  │  ├─ candidate-ranking/
│  │  ├─ sentence-decoder/
│  │  ├─ user-model/
│  │  ├─ engine-protocol/
│  │  └─ ime-ffi/
│  │
│  ├─ schemas/
│  │  ├─ xiaohe.json
│  │  └─ schema-format.json
│  │
│  ├─ tests/
│  │  ├─ fixtures/
│  │  ├─ parser_cases/
│  │  ├─ candidate_cases/
│  │  └─ sentence_cases/
│  │
│  └─ benches/
│
├─ tools/
│  ├─ lexicon-builder/
│  ├─ schema-validator/
│  └─ corpus-checker/
│
├─ dictionaries/
│  ├─ source/
│  ├─ generated/
│  └─ LICENSES/
│
├─ scripts/
│  ├─ check-environment.sh
│  ├─ build-rust.sh
│  ├─ copy-native-artifacts.sh
│  ├─ build-hap.sh
│  ├─ run-tests.sh
│  └─ clean.sh
│
├─ docs/
│  ├─ PROJECT_PLAN.md
│  ├─ ARCHITECTURE.md
│  ├─ PROJECT_STATE.md
│  ├─ CODING_RULES.md
│  ├─ API_CONTRACT.md
│  ├─ TEST_PLAN.md
│  ├─ PRIVACY_DESIGN.md
│  ├─ LEXICON_FORMAT.md
│  ├─ adr/
│  └─ diagrams/
│
├─ .cargo/
│  └─ config.toml
│
├─ build-profile.json5
├─ hvigorfile.ts
├─ oh-package.json5
├─ README.md
└─ CHANGELOG.md
```

------

# 五、核心数据流

## 5.1 普通按键流程

```text
用户点击字母键
    ↓
KeyButton 发送 KeyEvent
    ↓
KeyboardController
    ↓
InputSessionController
    ↓
EngineCoordinator
    ↓
NativeEngineGateway
    ↓
C++ Node-API
    ↓
Rust C ABI
    ↓
双拼解析
    ↓
词库查询与排序
    ↓
CompositionResult
    ↓
C++ 转换为 ArkTS 对象
    ↓
更新 InputSessionStore
    ↓
候选栏和组合文本刷新
```

## 5.2 候选词选择流程

```text
点击候选词
    ↓
CandidateController
    ↓
通知 Rust 选中候选
    ↓
Rust 更新用户词频和组合状态
    ↓
ArkTS 调用 IME Kit 提交文字
    ↓
刷新剩余组合和候选
```

## 5.3 删除流程

```text
点击删除键
    ↓
判断组合缓冲区
    ├─ 不为空：调用 Rust backspace
    └─ 为空：调用 IME Kit 删除编辑框字符
```

## 5.4 推荐统一返回对象

```typescript
interface CompositionResult {
  success: boolean
  errorCode: number
  errorMessage: string

  rawInput: string
  preeditText: string
  parsedSyllables: string[]

  candidates: Candidate[]
  highlightedIndex: number
  hasNextPage: boolean

  commitText: string
  compositionFinished: boolean
}
```

```typescript
interface Candidate {
  id: string
  text: string
  reading: string
  source: CandidateSource
}
```

正式版本不建议把内部排序分数暴露给普通 UI。Debug 构建可以额外返回评分信息。

------

# 六、状态管理设计

## 6.1 输入会话状态

统一由 `InputSessionStore` 管理：

```text
当前原始编码
当前预编辑文本
当前候选列表
当前候选页
当前键盘模式
当前编辑框类型
当前双拼方案
是否允许用户学习
引擎是否已加载
输入法是否可见
当前错误状态
```

## 6.2 禁止的状态管理方式

禁止：

- 每个按键组件保存一份组合文本；
- 候选栏自己查询 Native；
- ExtensionAbility 直接修改页面状态；
- 设置页面直接操作 Rust 句柄；
- 多个模块分别创建 Rust 引擎实例；
- 使用全局可变变量保存所有状态。

## 6.3 引擎实例生命周期

建议：

```text
ExtensionAbility 创建
    ↓
创建一个 Rust Engine Handle
    ↓
输入会话切换时 reset
    ↓
方案切换时重新加载配置
    ↓
ExtensionAbility 销毁时 destroy
```

------

# 七、开发阶段规划

## 阶段 0：技术可行性验证

### 目标

在编写正式功能前，验证三层链路能够真实运行。

### 实现内容

- 创建最小 HarmonyOS 输入法工程；
- 注册 InputMethodExtensionAbility；
- 显示一个只有几个按键的测试键盘；
- 实现文本插入；
- 实现文本删除；
- 创建最小 C++ Node-API 模块；
- ArkTS 调用 C++ 返回固定字符串；
- 创建最小 Rust 静态库或动态库；
- C++ 调用 Rust 函数；
- Rust 返回固定候选列表；
- 真机和模拟器分别验证；
- 记录完整工具链版本。

### 验收标准

```text
ArkTS → C++ → Rust → C++ → ArkTS
```

链路在真机成功运行。

必须产出：

- 工具链说明；
- Rust 编译脚本；
- CMake 配置；
- ABI 配置；
- 最小 Demo；
- 已知问题记录。

在这一阶段没有完成前，不得开始大规模开发词库和 UI。

------

## 阶段 1：工程骨架与规范建立

### 目标

建立正式工程结构，防止后期代码堆积。

### 实现内容

- 建立 ArkTS 分层目录；
- 建立 Rust Cargo Workspace；
- 建立 C++ Bridge 目录；
- 创建文档目录；
- 创建统一错误码；
- 创建统一日志接口；
- 配置代码格式化；
- 配置单元测试；
- 配置构建脚本；
- 固定 SDK、Rust 和工具链版本；
- 创建 `PROJECT_STATE.md`；
- 创建架构决策记录 ADR。

### 验收标准

- 三层均可独立编译；
- 一条命令可以运行 Rust 测试；
- 一条命令可以构建 Native 库；
- 一条命令可以构建 HAP；
- README 中写清环境配置和构建方法。

------

## 阶段 2：输入法基础壳

### 目标

完成不包含双拼算法的基础输入法。

### 实现内容

- ExtensionAbility 生命周期；
- 输入法窗口创建；
- 键盘显示和隐藏；
- 普通文本提交；
- 删除；
- 空格；
- 回车；
- 中英文切换占位；
- 输入会话开始和结束；
- 基础错误处理。

### 验收标准

可以作为输入法启用，并在普通输入框中完成英文输入、删除、空格和回车。

------

## 阶段 3：键盘 UI 工程化

### 目标

建立可以长期维护的键盘组件系统。

### 实现内容

- 统一键盘尺寸系统；
- 统一按键样式；
- 字母按键组件；
- 功能按键组件；
- 删除长按；
- 按键按下效果；
- 按键弹出预览；
- 候选栏占位；
- 横屏适配；
- 深色模式；
- 安全区域适配。

### 验收标准

- 不同键盘布局复用基础组件；
- 页面中不存在大量复制粘贴的按键代码；
- 横竖屏均能正常显示；
- 长按删除稳定。

------

## 阶段 4：Rust 双拼方案与解析器

### 目标

实现不依赖词库的双拼编码解析。

### 实现内容

- 双拼配置格式；
- 小鹤双拼配置；
- 配置校验器；
- 双拼按键解析；
- 拼音音节表；
- 合法音节校验；
- 不完整编码处理；
- 非法编码处理；
- 回退和清空；
- 解析器状态机；
- 单元测试。

### 验收标准

为每个映射规则准备测试用例，并建立至少以下测试类型：

- 正常音节；
- 零声母；
- 非法组合；
- 不完整组合；
- 连续多个音节；
- 删除回退；
- 方案切换。

------

## 阶段 5：正式跨语言接口

### 目标

建立稳定的 ArkTS、C++、Rust 接口合同。

### 实现内容

- Rust 引擎句柄；
- C ABI；
- C++ RAII 句柄管理；
- ArkTS 类型声明；
- 参数校验；
- UTF-8 转换；
- 错误码映射；
- 内存释放；
- Engine 版本查询；
- 接口版本检查；
- 异常恢复。

### 验收标准

- 连续创建和销毁引擎不泄漏；
- 错误参数不会导致崩溃；
- Rust panic 不得穿过 FFI 边界；
- 所有 Rust 分配的跨语言内存都有对应释放函数；
- ArkTS 不接触裸指针。

------

## 阶段 6：词库构建系统

### 目标

建立可重复生成的二进制词库。

### 实现内容

- 定义原始词库格式；
- 词库版权和来源记录；
- 词库清洗；
- 拼音规范化；
- 重复词合并；
- 基础词频处理；
- 索引构建；
- 二进制格式；
- 词库版本；
- 校验和；
- 词库构建命令；
- 小型测试词库。

### 验收标准

相同输入必须生成一致的词库文件。

词库构建失败时必须指出：

- 错误行；
- 错误词条；
- 错误拼音；
- 错误原因。

------

## 阶段 7：候选查询与基础排序

### 目标

完成可使用的中文候选功能。

### 实现内容

- 单字查询；
- 词语查询；
- 前缀查询；
- 候选数量限制；
- 候选分页；
- 基础词频排序；
- 完整匹配优先；
- 去重；
- 候选结果缓存；
- 候选栏接入。

### 验收标准

用户输入双拼编码后，可以看到正确候选，并能点击候选提交到编辑框。

------

## 阶段 8：组合输入和短句解码

### 目标

支持连续双拼输入，而不是每两个字母必须立即选词。

### 实现内容

- 多音节解析；
- 音节切分图；
- 词语路径生成；
- 短句候选；
- Beam Search 或 Viterbi；
- 首选整句；
- 部分候选提交；
- 剩余编码继续转换；
- 句子状态回退。

### 验收标准

连续输入多个双拼编码后，可以得到合理的词组或短句候选。

### 当前验收记录

2026-07-09 已在 x86_64 HarmonyOS 模拟器完成设备验收：`nihc` 可显示并提交 `你好`，`uurufa` 可显示 `输入法` 与部分候选 `输入`，选择 `输入` 后剩余 `fa` 可继续提交 `法`。

------

## 阶段 9：用户词频学习

### 目标

让候选顺序逐渐适应用户。

### 实现内容

- 用户词频模型；
- 选择次数统计；
- 最近使用加权；
- 用户词与系统词区分；
- 用户模型文件；
- 原子写入；
- 延迟刷盘；
- 数据压缩；
- 数据损坏恢复；
- 清空用户数据；
- 密码场景禁用学习。

### 验收标准

连续选择同一个非首选候选后，该候选能够逐渐上升，但不能无限覆盖系统基础排序。

### 当前验收记录

2026-07-09 已在 x86_64 HarmonyOS 模拟器完成设备验收：`ni` 初始排序为 `你`、`尼`、`泥`；重复选择 `泥` 后排序变为 `你`、`泥`、`尼`，验证用户学习有界上升；点击 `隐藏` 刷新用户模型并强制重启进程后，学习排序仍保留。

------

## 阶段 10：输入框类型与键盘模式

### 目标

根据不同编辑框自动选择合适布局。

### 实现内容

- 普通文本模式；
- 密码模式；
- 数字模式；
- 电话模式；
- 邮箱模式；
- URL 模式；
- 搜索模式；
- 回车键图标和行为切换；
- 中文、英文、数字和符号切换；
- 模式切换状态恢复。

### 验收标准

不同输入框中显示正确键盘布局，密码模式不保存用户输入和学习记录。

------

## 阶段 11：设置、主题、震动和音效

### 目标

完成输入法基础个性化。

### 实现内容

- 设置主页；
- 双拼方案选择；
- 主题选择；
- 键盘高度；
- 候选字号；
- 震动强度；
- 按键音；
- 用户学习开关；
- 清空用户词频；
- 设置即时生效；
- 设置数据迁移。

### 验收标准

修改设置后，无需重启输入法即可正确生效；异常配置能够回退到默认值。

------

## 阶段 11.5：生产级词库接入与中文覆盖验收

### 目标

接入生产级中文词库，替换当前的小型测试词库，使输入法具备日常使用级中文输入能力，并为后续性能优化提供真实的词库规模和运行数据。

### 实现内容

- 选择许可证明确的中文词库来源；
- 导入常用单字；
- 导入常用双字词和多字词；
- 导入常用成语和短句；
- 处理多音字；
- 处理简体字和生僻字；
- 建立基础词频；
- 生成正式二进制词库；
- 替换目前的小型测试词库；
- 建立几千至几万条回归语料；
- 测试常见日常中文输入；
- 检查 HAP 体积、加载时间和内存占用。

### 验收标准

- 所有词库来源均具有明确许可证，并在 `dictionaries/LICENSES/` 中保存来源、许可证和使用说明；
- 正式二进制词库能够通过现有词库构建工具稳定、可重复地生成；
- 应用运行时不再依赖小型测试词库，默认加载正式词库；
- 常用单字、双字词、多字词、成语和短句能够正常查询和提交；
- 多音字能够按照不同读音进入正确候选，并具备合理的基础排序；
- 简体常用字和项目确定范围内的生僻字能够正常输入；
- 建立至少几千条、目标达到几万条的固定回归语料，并能够自动执行；
- 常见日常中文输入测试通过，候选结果不存在大面积缺词、错音或明显排序异常；
- 记录正式词库接入后的 HAP 安装包体积、输入法启动时间、词库加载时间和内存占用；
- 词库规模、覆盖率和性能基线形成验收报告，作为阶段 12 性能优化的输入依据。

完成本阶段后，项目才能称为具备日常使用级中文输入能力。

------

## 阶段 11.6：正式小鹤音形双链路（2026-07-22 重启）

> 状态：**ACTIVE / 11.6.9 PARTIALLY COMPLETED（2026-07-27）**。11.6.2B～11.6.8 已完成；11.6.9 主机、构建、Release 与主机性能门禁通过，但受保护密码键盘无法由现有 `uitest` 脚本识别，且 ARM64 物理真机、signed Release 独立 TextInput、设备性能和完整压力矩阵尚未执行。经理已明确确认本项目可以使用根目录“码表”和“小鹤音形”中的交付文件，客户已同意采用该路线。

### 总体目标

在不改变现有小鹤双拼行为的前提下，将交付的小鹤音形主码表、分类词库和配置规则转换为可审计、可重复构建、可随 HAP 安装的正式 bundle，并完成查询、分类、用户覆盖、引导、四码提交状态、设置入口、资源安装和真机发布验收。

最终同时提供两条隔离的正式链路：

| 方案 | 稳定方案 ID | Rust 后端 | 数据与排序 |
| --- | --- | --- | --- |
| 小鹤双拼 | `xiaohe` | `Shuangpin` | 现有双拼 schema、生产拼音词库、整句解码、`ExistingRanking` 和用户学习 |
| 小鹤音形 | `xiaohe-yinxing` | `CodeTable` | 正式小鹤音形 bundle、原始编码、分类顺序、`source_order` 和用户硬规则 |

两条链路只共享 `CompositionResult`、候选会话、分页、C ABI、Node-API、ArkTS 候选栏和用户文件存储外壳；不得共享解析、基础排序或组合状态。音形资源错误不得改变或污染 `xiaohe`。

### 当前基础与重启起点

已经完成且不重复开发：

- **11.6.1**：`ime-engine` 下的 `Shuangpin/CodeTable` 双后端边界、空字符串/空码、精确优先、前缀回退、分类顺序和 reset 合同；
- **11.6.2A**：原创 fixture、严格文本校验、确定性分类 bundle、损坏检测和 Release fixture 排除门禁；
- **11.6.3 通用部分**：`code-table-runtime` 的原始小写编码、`ExactOrPrefix + SourceOrder`、分类合并、完整候选后分页和资源错误；
- **11.6.4 通用部分**：用户 `#删/#固/#N`、普通新增、不可变快照、主备份恢复和码表后端覆盖。

已完成：正式交付数据的权威清单与转换、冻结正式 bundle、正式系统查询，以及包内 36 条固定规则与外部用户词库的确定性覆盖。

尚未完成：11.6.9 的设备阻断修复、ARM64 物理真机、signed Release 独立
TextInput、设备性能和完整发布验收。

**本次重启必须从 11.6.2B 开始。** 不从 UI 开始，不先修改按键状态机，也不把原始 txt 放入 HAP。第一件事是冻结正式输入文件、优先级、哈希和转换合同。

### 正式交付数据范围

#### 核心主码表

- `小鹤音形/0.0.小鹤.txt` 是核心主码表候选；
- 约 68,507 条数据记录，普通音形编码最长四码；
- 配置头、注释和命令行由离线转换器显式处理，运行时不得直接解析原始 txt。

#### 分类与覆盖资料

根目录 `码表` 和 `小鹤音形` 中的以下类型作为正式上游候选：

- 分类/次选、一简次选、二简次选；
- 表外字、全码词、生僻字、全码字；
- 用户新增、删除、固顶和指定位置；
- 快符、符号、符号组、直通；
- 拼字、简繁转换和 Android 配置参考。

同名或同用途文件不得自动合并。11.6.2B 必须为每类指定唯一权威文件、补充文件、优先级和冲突规则，目录扫描顺序不得成为产品顺序。

#### 配置参考边界

`小鹤音形/ime.android.ini` 只作为行为与分类参考：

- 可提取原始序、分类组合、自动上屏、顶屏、空码、候选数量和文件映射；
- 网络、WebDAV、API、账号、外部程序和平台私有配置不进入项目配置；
- 文件内凭据不得进入正式资源、日志、测试快照或 HAP；
- Android/Windows 命令必须转换成 HarmonyOS 明确支持的动作，无法映射的命令形成拒绝报告。

### 数据分层

| 数据类型 | 目标层 | 要求 |
| --- | --- | --- |
| 普通 `词条<TAB>编码` | 系统分类表 | 保留稳定原始顺序，禁止频率重排 |
| `#删/#固/#N` | 用户覆盖层 | 与系统表分离；复合标记转换成确定操作序列 |
| `$cmd/$ddcmd` | 功能动作表 | 白名单解析，不作为普通候选文本导入 |
| 普通符号/文本 | 符号或快捷分类 | 允许进入专用分类 |
| `ok...` 拼字 | 独立拼字能力 | 不混入普通四码核心表 |
| `hans2hant` | 简繁转换资源 | 不作为音形候选表 |
| 平台命令 | 拒绝或适配层 | 禁止外部程序和未经设计的网络动作 |

### 分层职责

- **ArkTS**：方案选择、分类开关、设置持久化、快捷键路由和执行协议动作；不解析码表、不排序；
- **C++**：UTF-8、路径、长度、句柄、错误映射和生命周期；不实现音形规则；
- **Rust 离线工具**：规范化、分类 manifest、用户规则转换、命令白名单、拒绝报告和确定性 bundle；
- **Rust 运行时**：bundle 校验、原始编码状态机、分类快照、候选合并、用户覆盖、引导和提交决策；
- **构建门禁**：区分 fixture、原始交付文件和正式 bundle；批准前只允许 Debug 加载，禁止原始 txt/ini 进入 HAP。

---

### 子阶段 11.6.1：双后端与行为合同

状态：**COMPLETED；重启后保留。**

重启补充：

- 正式音形 ID 固定为 `xiaohe-yinxing`；
- `xiaohe` 继续是默认方案和故障安全回退；
- 音形后端不经过双拼 parser、拼音词库、整句解码、`ExistingRanking` 或 `user-model`；
- 切换方案、会话结束、编辑框变化和资源异常均清理音形组合及候选；
- 现有 ABI/interface version 2 能承载的行为优先复用，确需新增跨层动作时先升级合同。

验收：双拼回归不变；音形资源缺失不导致双拼不可用；组合、页码和候选不串扰。

### 子阶段 11.6.2A：原创 fixture 与构建门禁

状态：**COMPLETED；继续作为测试基础。**

保留 fixture、确定性测试 bundle、格式损坏用例和 Release 排除门禁。fixture 只验证通用引擎，不作为正式降级资源，也不进入正式设置页。

### 子阶段 11.6.2B：正式数据接收、权威清单与审计

状态：**COMPLETED（2026-07-22）。**

实现内容：

1. 为两个交付目录生成接收清单；
2. 记录路径、作用、字节数、行数、编码、换行和 SHA-256；
3. 统计重复分类的完全相同、部分重叠、仅一侧存在和语法差异；
4. 指定每个分类的权威文件、补充文件和稳定合并顺序；
5. 固定经理确认、客户确认、接收日期和内部审批记录；
6. 识别普通记录、用户规则、命令、配置、简繁和拼字；
7. 隔离凭据、网络地址、账号、外部程序命令和平台键码；
8. 输出接受、转换、延期和拒绝四类报告；
9. 冻结 scheme ID、bundle ID、数据版本和转换器版本。

交付物：

- 正式来源 manifest 和文件级 SHA-256；
- 分类映射与优先级表；
- 命令白名单与拒绝规则；
- 缺失配置引用清单；
- 安全清理报告；
- 供 11.6.2C 使用的机器可读转换合同。

验收：不依赖目录扫描或猜测；任意输出记录可追溯到文件和物理行；冲突有可测试结论；原始文件不进入 HAP；完成前不得开放正式 UI。

### 子阶段 11.6.2C：正式规范化与生产 bundle

状态：**COMPLETED（2026-07-22）；正式 bundle、确定性构建、完整性与基础查询已验收。**

实现内容：

- 新增独立正式转换器，不修改 fixture 生成器；
- 支持 UTF-8/BOM、LF/CRLF、注释和配置头；
- 普通记录规范化为严格 `词条<TAB>编码`；
- 用户新增、`#删/#固/#N` 和复合状态转换到用户合同；
- 安全文本、符号和功能动作分流；
- 为扩展区字符、Emoji、成对符号和长文本建立验证策略；
- 生成 manifest、分类二进制、动作元数据和总 bundle；
- 输出输入、接受、转换、拒绝、重复和冲突统计；
- 两个干净目录重复构建并比较字节与 SHA-256；
- 增加篡改、截断和版本不兼容测试。

首版必须包含核心主表、分类/次选、一简、二简、表外字、全码词、生僻字和全码字。拼字、完整符号组、全部直通、简繁、暗词库、纠错和英文补全可延期，但必须明确记录。

验收：正式 bundle 可完全重建；双构建一致；拒绝原因稳定；Debug 可基础查询；产品门禁通过前 Release 仍拒绝 bundle。

### 子阶段 11.6.3：正式系统码表查询回归

状态：**COMPLETED（2026-07-22）。**

- 覆盖单码到四码、精确优先、前缀、分类顺序、`source_order` 和稳定去重；
- 验证空字符串、大重码、最大候选和完整列表后分页；
- 切回 `xiaohe` 后拼音候选和整句行为不变；
- 记录加载耗时、内存和逐键查询 P50/P95；
- 资源损坏返回明确错误并安全回退双拼。

验收结果：冻结 `HSPYXP01` 正式包 25,397,952 bytes/SHA-256 `00c7d5a9d6b74a079a7434df23f510aa348fe8fee5bf1eaf72e691d68bcd1e30`，八类 73,263 条；23 个独立快照、全损坏矩阵、Rust/FFI 跨层回归、Release 性能和真实 ArkTS → C++ → Rust 设备双轮 A～J 均通过。正式包仍为 Debug-only 临时资源，Release HAP 继续只包含 `production.lex`。

### 子阶段 11.6.4：正式用户规则转换与覆盖

状态：**COMPLETED（2026-07-23）。**

- 冻结普通新增、删除、固顶、指定位置和复合标记语义；
- 必要时把复合记录拆成确定操作序列；
- 保持完整编码＋词条精确删除、多个固顶、固顶保护和 1-based 全局位置；
- 用户规则作用于完整候选后再分页；
- 系统表不可变，用户文件损坏继续主/备份恢复；
- 不支持的复合语义必须拒绝，不能静默猜测。

验收结果：冻结包内 `user-rules.txt` 的 36 条规则全部为 `#固`，运行时严格加载后作为内置基础层；可选外部 `userLexiconPath` 作为第二层，同完整编码＋词条由外部规则覆盖。两层只在引擎创建时合并成不可变快照，候选执行继续复用既有硬排序算法。Rust/FFI、ArkTS 288/288、双 ABI、Release 门禁及 x86_64 模拟器强停重启两轮 A～J 全 PASS；interface/ABI version 保持 2。

### 子阶段 11.6.5：产品分类、默认开关与顺序

状态：**COMPLETED（2026-07-23）。**

- 冻结分类 ID、名称、权威文件、默认开关和顺序；
- 区分主表、视同主表、扩展、用户和功能分类；
- ArkTS 提供产品确认的分类开关；
- Rust 每次操作捕获不可变分类快照；
- 分类变化无提交 reset、清页码并重算；
- 旧设置和未知分类进行确定迁移；
- 分类关闭同时影响候选、后续码和自动提交唯一性。

### 子阶段 11.6.6：引导、快符、符号与安全直通

状态：**RUNTIME COMPLETED / PRODUCTION DATA PARTIALLY DEFERRED（2026-07-23）。**

实现顺序：普通文本/符号 → 快符 → 日期时间 → 成对符号/光标 → 引导状态 → 其他批准直通。

禁止运行外部程序、未经设计的网页/网络动作、携带账号或 API、使用无法映射的平台键码。引导表与普通分类物理隔离；空引导码不枚举全表；退格、连续引导键、非法键、空格、回车和候选选择都有唯一结果；一次按键最多执行一个动作。

### 子阶段 11.6.7：四码提交、顶屏与空码切分

状态：**COMPLETED（2026-07-24）。**

已完成全部范围：四码最终唯一自动上屏、多候选保留、有效更长编码保护、第五键顶屏与新键单次重放、旧段无首选时不丢新键、空码正向/反向切分、空码清屏、分类/用户规则参与唯一性、ABI 4 跨层单次提交和状态序列回归。

空码切分规则已于2026-07-24经产品授权冻结并完整实现（ADR 0018）：
- 正向切分：从右向左扫描非空边界，选择具有最终精确候选的最长左前缀，提交该前缀首选，右侧原始编码作为剩余段。
- 反向切分：若正向无解，从左向右扫描，选择具有最终可见精确候选或有效更长编码的最长右后缀；左侧无法解释的ASCII原样作为唯一`commitText`，右后缀作为剩余段。
- 正向优先于反向；相同输入和快照结果唯一；切分成功后只产生一个提交，剩余段只处理一次；未读取或复制`ime.android.ini`。

依据正式表和冻结配置形成的表驱动状态机已实现并测试：

- 四码唯一自动上屏、四码多候选保留；
- 有更长有效编码时不得误上屏；
- 第五码到来前旧段顶屏，当前键作为新段首键恰好处理一次；
- 旧段无首选时清旧段但不丢当前键；
- 空码清屏与空码切分优先级、正反向切分和最大长度；
- 用户删除、分类变化对唯一性与有效更长码的影响；
- 空格、回车、退格、翻页、候选选择和模式切换；
- 一次操作最多一个非空 `commitText`。

如需多段组合或额外跨层动作，先更新 API、ABI 和测试，不在 ArkTS 中临时拼凑。

### 子阶段 11.6.8：正式方案、设置和资源接入

状态：**COMPLETED（主机与 x86_64 模拟器范围，2026-07-27）。**

- Rust/FFI/C++/ArkTS 正式支持 `xiaohe-yinxing`；
- 设置页同时展示“小鹤双拼”和“小鹤音形”；
- 默认保持 `xiaohe`，用户切换后持久化；
- 从 HAP 原子安装并校验版本化 bundle；
- 缺失、损坏或不兼容时提示并回退 `xiaohe`；
- fixture 不得作为生产回退；
- 批准前只允许 internalDebug 加载；
- 建立旧设置、未知 scheme ID 和 bundle 版本的幂等迁移；
- 正式资源不包含原始 txt/ini、凭据或离线研究数据。

复验补充：设置与输入法扩展处于不同进程，方案快照通过本包 `SHARED_CONFIG`
DataProxy 同步；正式 Native handle 只在 bundle 安装、大小/SHA 校验及新 handle 创建成功后交换。
signed Release 独立 TextInput A～F 与 internalDebug G～K（含旧设置迁移）均通过，实际
HAP 扫描及负向篡改门禁通过。架构见 ADR 0019，证据见
`docs/evidence/2026-07-27-stage-11.6.8-revalidation/`。

### 子阶段 11.6.9：全链路、性能与真机发布验收

状态：**PARTIALLY COMPLETED（2026-07-27）。**

实际结果：Rust 387/387、FFI 26/26、ArkTS 323/323、fmt/Clippy、双 ABI Native、
三类 HAP、Release 正负门禁、签名验证、28/28 来源不可变、15/15 双构建和五进程
主机性能测量 PASS。Phone/Pad x86_64 模拟器的密码场景只暴露受保护表面，现有脚本
未识别系统安全键盘并在应用布局分支报“无 Shift”；设置脚本
修正 DebugIndex 入口后仍未完成清空用户模型后半程。signed Release 独立 TextInput、
ARM64 物理真机、设备性能与完整稳定性/应用矩阵为 NOT RUN。证据见
`docs/evidence/2026-07-27-stage-11.6.9-final-acceptance/`。

自动化门禁：

- 来源清单、SHA-256、转换器和双构建；
- bundle 完整性、查询、分类、用户覆盖、引导和提交状态；
- 双后端隔离、C ABI、Node-API、ArkTS、设置迁移和资源安装；
- 小鹤双拼全量回归；
- Release HAP 的资源、权限、凭据、fixture 和原始文件扫描。

设备覆盖：

- phone/tablet 模拟器和至少一台 ARM64 真机；
- 聊天、搜索、浏览器、多行、横竖屏和应用切换；
- 单码到四码、重码、翻页、自动上屏、顶屏、空码切分；
- 分类、用户规则、引导、快符、符号和已批准直通；
- 方案切换、重启、资源损坏和安全回退。

记录 bundle/HAP 大小、加载时间、内存、逐键 P50/P95、大重码分页、方案切换和 reset。只有正式 bundle 可重建、全部声明功能有自动化与设备证据、Release 无 fixture/原始文件/凭据/网络权限、双链路无串扰并通过客户与发布验收后，阶段 11.6 才标记 `COMPLETED`。

### 重启后的立即执行清单

按顺序启动，不并行越级：

1. 创建 11.6.2B 正式接收 manifest，只做读取、哈希、统计和分类；
2. 确认两个目录中分类、全码词、符号、直通和用户调整的权威关系；
3. 冻结首版功能范围，决定拼字、完整直通、简繁和暗词库是否延期；
4. 冻结命令白名单，危险或平台专用命令拒绝；
5. 形成转换合同和拒绝报告格式；
6. 实现正式转换器与 bundle；
7. 用正式数据回归 11.6.3/11.6.4；
8. 依次完成 11.6.5～11.6.9，不得先开放正式设置入口。

因此，本次重启的第一项任务是：

> **子阶段 11.6.2B：把经理交付的两个目录冻结成一份权威、可追溯、可转换的正式数据合同。**


## 阶段 12：性能优化

### 内部性能目标

建议设定：

- 输入法窗口可交互时间：不超过 800 毫秒；
- 首次按键响应 P95：不超过 30 毫秒；
- 后续按键到候选刷新 P95：不超过 16 毫秒；
- 候选点击到文本提交 P95：不超过 30 毫秒；
- 普通输入期间不发生明显掉帧；
- 用户模型保存不得阻塞 UI；
- 词库不得在每次按键时重复加载。

### 优化内容

- 词库预加载；
- 二进制索引；
- 查询缓存；
- 候选数量限制；
- 避免每次按键创建大量对象；
- 减少跨语言调用次数；
- 避免频繁 JSON 深层嵌套；
- 用户模型批量写入；
- 删除长按事件节流；
- ExtensionAbility 启动分析。

所有优化必须以 Profiler 或基准测试数据为依据。

------

## 阶段 13：稳定性、安全与隐私

### 实现内容

- 无网络权限检查；
- 输入日志脱敏；
- 密码场景保护；
- Rust panic 捕获；
- Native 内存泄漏检查；
- 词库损坏恢复；
- 用户数据损坏恢复；
- 极端长输入限制；
- 高频按键压力测试；
- ExtensionAbility 重建测试；
- 前后台切换测试；
- 应用切换测试；
- 横竖屏切换测试；
- 低内存恢复；
- 崩溃日志分类。

### 隐私规范

禁止记录：

- 用户完整输入内容；
- 密码输入；
- 输入框原始文本；
- 候选选择对应的完整上下文；
- 可以还原个人敏感信息的日志。

Debug 日志也必须遵循脱敏原则。

------

## 阶段 14：发布准备

### 实现内容

- 应用图标；
- 输入法名称；
- 使用说明；
- 双拼学习说明；
- 隐私政策；
- 用户数据说明；
- 权限检查；
- 签名配置；
- Release 构建；
- 混淆和符号文件；
- 崩溃定位方案；
- 版本升级测试；
- 真机兼容测试；
- 上架材料；
- 第三方词库许可证清单。

------

# 八、测试体系

## 8.1 Rust 单元测试

重点测试：

- 双拼映射；
- 音节合法性；
- 连续编码切分；
- 非法输入；
- 删除回退；
- 词库查询；
- 排序稳定性；
- 整句路径；
- 用户词频；
- 用户数据迁移；
- 损坏数据恢复。

## 8.2 Rust 属性测试与模糊测试

对解析器执行随机输入：

- 任意 ASCII；
- 超长字符串；
- 非 UTF-8 边界数据；
- 连续删除；
- 反复切换方案；
- 不完整编码。

要求：

- 不 panic；
- 不越界；
- 不死循环；
- 不产生无法释放的内存。

## 8.3 C++ 桥接测试

测试：

- 空参数；
- 错误类型；
- 无效句柄；
- 重复销毁；
- Rust 错误码；
- 超长返回值；
- UTF-8 中文；
- Native 内存释放；
- 多次创建和销毁。

## 8.4 ArkTS 测试

测试：

- 状态管理；
- 键盘模式切换；
- 删除策略；
- 回车策略；
- 编辑框类型映射；
- 设置读取；
- 设置回退；
- Native 异常展示；
- 候选点击。

## 8.5 真机集成测试

至少覆盖：

- 备忘录；
- 浏览器搜索框；
- 聊天输入框；
- 密码输入框；
- 数字输入框；
- 电话输入框；
- 邮箱输入框；
- 多行输入框；
- 横屏；
- 深色模式；
- 快速切换应用；
- 连续输入五分钟；
- 长按删除；
- 快速点击候选。

## 8.6 回归语料库

建立固定测试语料：

```text
双拼编码
预期音节
预期首选候选
允许出现的候选
禁止出现的候选

```

每次修改解析、词库或排序后自动运行，避免修复一个词后破坏其他词。

------

# 九、编码规范

## 9.1 ArkTS 规范

- UI、状态、系统服务和 Native 调用必须分层；
- 页面组件不直接调用 C++；
- 禁止使用大量全局变量；
- 禁止在 `build()` 中编写复杂业务逻辑；
- 公共类型统一放入 domain 或 contract；
- 系统 API 统一由 infrastructure 封装；
- 一个组件只承担一个明确职责；
- 所有异步调用必须处理异常；
- 日志统一使用封装后的 Logger。

## 9.2 Rust 规范

- 使用 Cargo Workspace；
- 每个 crate 职责单一；
- `unsafe` 主要限制在 FFI crate；
- 核心算法 crate 原则上禁止 `unsafe`；
- 所有公开 API 有文档；
- 所有错误使用明确错误类型；
- FFI 边界捕获 panic；
- 不在核心模块直接依赖鸿蒙 API；
- 词库格式必须有版本；
- 排序算法必须可重复测试。

## 9.3 C++ 规范

- 使用 RAII；
- 禁止裸 `new/delete` 管理长期对象；
- 所有句柄检查有效性；
- 所有 Node-API 调用检查返回状态；
- 所有 Rust 缓冲区及时释放；
- 业务算法不得进入 C++；
- Native 导出接口集中管理；
- 头文件和实现文件分离；
- 不允许一个 `napi_init.cpp` 承担全部逻辑。

## 9.4 文件规模原则

不设置绝对死板限制，但出现以下情况必须拆分：

- 一个文件承担两个以上领域职责；
- 一个类同时处理 UI、存储和 Native；
- 一个函数超过一个完整屏幕仍无法概括；
- 出现大量重复条件判断；
- 修改一个功能经常需要同时改动无关代码；
- 测试无法单独构造目标模块。

------

# 十、构建与持续集成

## 10.1 工具链固定

项目必须记录：

- DevEco Studio 版本；
- HarmonyOS SDK 版本；
- API 版本；
- NDK 版本；
- CMake 版本；
- Rust 版本；
- Rust target；
- Cargo 依赖版本；
- Node/ohpm 版本；
- 真机系统版本。

禁止在文档中只写“使用最新版”。

## 10.2 推荐构建流程

```text
检查开发环境
    ↓
执行 cargo fmt
    ↓
执行 cargo clippy
    ↓
执行 cargo test
    ↓
交叉编译 Rust Native 库
    ↓
复制 Rust 产物
    ↓
编译 C++ Node-API 模块
    ↓
执行 ArkTS 测试
    ↓
构建 HAP
    ↓
安装到测试设备

```

## 10.3 Windows 开发建议

如果 DevEco Studio 运行在 Windows：

- DevEco Studio 和 HarmonyOS 工程运行在 Windows；
- Rust 交叉编译优先固定在一种确定环境中；
- 可以单独使用 WSL2 或 Linux 构建 Rust 产物；
- Rust 产物通过脚本复制到工程；
- 不要一部分依赖装在 Windows、一部分装在 WSL，却没有文档说明；
- 构建脚本必须明确自己运行在哪个环境；
- CI 与本地构建使用相同 Rust toolchain。

## 10.4 每次提交检查

必须通过：

```text
ArkTS 编译
C++ 编译
Rust fmt
Rust clippy
Rust test
FFI 测试
核心回归语料测试
Debug HAP 构建

```

------

# 十一、Vibe Coding 开发规则

## 11.1 每轮只完成一个阶段

禁止一次让 AI：

```text
直接完成整个双拼输入法

```

正确方式：

```text
阶段 0.1：只建立 InputMethodExtensionAbility
阶段 0.2：只实现文本提交和删除
阶段 0.3：只验证 ArkTS 调用 C++
阶段 0.4：只验证 C++ 调用 Rust

```

## 11.2 每轮必须提供的文件

每次交给 Codex 或其他 AI 时，应至少提供：

```text
PROJECT_PLAN.md
ARCHITECTURE.md
PROJECT_STATE.md
CODING_RULES.md
API_CONTRACT.md
当前阶段涉及的源代码
当前错误日志

```

## 11.3 AI 每轮必须输出

要求 AI 完成后报告：

1. 阅读了哪些文件；
2. 修改了哪些文件；
3. 新增了哪些文件；
4. 为什么这样设计；
5. 执行了哪些命令；
6. 哪些测试通过；
7. 哪些测试未执行；
8. 是否存在遗留问题；
9. 是否修改了架构边界；
10. 更新后的 `PROJECT_STATE.md`。

## 11.4 AI 禁止事项

- 禁止把所有代码写进一个文件；
- 禁止未经允许更换技术架构；
- 禁止在 C++ 中加入双拼算法；
- 禁止在 ArkTS 中加入候选排序；
- 禁止删除测试绕过错误；
- 禁止伪造测试通过；
- 禁止遇到编译错误后只注释代码；
- 禁止为了快速运行使用大量硬编码；
- 禁止引入来源不明的词库；
- 禁止未经说明增加第三方依赖；
- 禁止大范围重构与当前阶段无关的代码。

## 11.5 单轮提示词模板

```text
你是一名资深 HarmonyOS、ArkTS、C++ 和 Rust 工程师。

当前项目是一款纯血鸿蒙双拼输入法，严格采用以下分层：

1. ArkTS 负责鸿蒙系统层、IME Kit、ExtensionAbility、键盘 UI、
   候选栏、设置页面、输入框类型识别、文本提交删除、
   生命周期、震动、音效和主题。

2. Rust 负责双拼方案配置、双拼解析、拼音音节校验、
   词库索引、候选查询、候选排序、整句解码和用户词频。

3. C++ 只负责 ArkTS 与 Rust 的桥接、参数转换、错误码、
   返回对象转换和跨语言内存管理，不得实现业务算法。

请先阅读：
- PROJECT_PLAN.md
- ARCHITECTURE.md
- PROJECT_STATE.md
- CODING_RULES.md
- API_CONTRACT.md

当前阶段：
【填写阶段名称】

本轮唯一目标：
【填写唯一目标】

本轮必须实现：
【列出功能】

本轮禁止实现：
【列出暂不实现内容】

工程约束：
- 不得把大量逻辑堆积在一个文件；
- 每个模块职责必须单一；
- 不得破坏 ArkTS → C++ → Rust 的依赖方向；
- 不得在 C++ 中编写输入法业务算法；
- 不得在 ArkTS 中编写双拼解析和候选排序；
- 新增依赖必须说明原因；
- 必须补充对应测试；
- 不得伪造测试结果；
- 遇到无法执行的测试必须明确说明。

执行完成后输出：
1. 修改文件清单；
2. 核心实现说明；
3. 执行的命令；
4. 测试结果；
5. 尚未解决的问题；
6. 下一阶段建议；
7. 更新 PROJECT_STATE.md。

```

------

# 十二、版本规划

## 12.1 MVP 版本

MVP 必须包含：

- 输入法启用和显示；
- 小鹤双拼；
- 基础中文词库；
- 候选栏；
- 候选选择；
- 候选翻页；
- 中文、英文、数字、符号模式；
- 删除、空格和回车；
- 输入框类型识别；
- 基础设置；
- 深色模式；
- 按键震动；
- 本地用户词频；
- 基础隐私保护；
- 真机稳定运行。

## 12.2 1.1 版本

可以增加：

- 微软双拼；
- 自然码；
- 搜狗双拼；
- 自定义双拼方案；
- 模糊音；
- 更完整的整句解码；
- 用户词导入导出；
- 更多主题；
- 平板布局。

## 12.3 后续版本

可以考虑：

- 中英文混输；
- Emoji；
- 剪贴板；
- 常用语；
- 拼音纠错；
- 自定义词库；
- 本地语言模型；
- 多设备词库同步；
- AI 联想。

AI、云同步和剪贴板功能涉及更高隐私风险，不进入第一版。

------

# 十三、主要风险与应对措施

## 13.1 Rust 交叉编译风险

风险：

- Rust target 与具体 HarmonyOS SDK 不兼容；
- 静态库或动态库无法被 CMake 正确链接；
- 模拟器和真机 ABI 不一致；
- Rust 第三方 crate 依赖不兼容。

措施：

- 阶段 0 首先验证；
- MVP 先支持 `arm64-v8a`；
- 模拟器再补充 `x86_64`；
- 控制 Rust 第三方依赖；
- 优先选择纯 Rust crate；
- 避免早期引入复杂 C 系依赖；
- 固定工具链；
- 保留最小跨语言 Demo 作为回归工程。

## 13.2 输入法生命周期风险

风险：

- 应用切换后状态残留；
- 编辑框切换后候选错误；
- ExtensionAbility 重建；
- 键盘隐藏后仍保存旧组合。

措施：

- 建立明确输入会话状态机；
- 每个生命周期事件有测试；
- 区分引擎实例和输入会话；
- 会话结束时执行受控 reset。

## 13.3 词库性能风险

风险：

- 词库过大；
- 启动时间过长；
- 每次按键查询过慢；
- 内存占用过高。

措施：

- 离线生成二进制词库；
- 建立紧凑索引；
- 延迟加载低频数据；
- 限制首屏候选数量；
- 使用基准测试驱动优化。

## 13.4 AI 生成代码失控风险

风险：

- 架构被随意修改；
- 逻辑重新堆回单文件；
- 测试被删除；
- AI 声称完成但没有真实运行。

措施：

- 小阶段开发；
- 强制更新 `PROJECT_STATE.md`；
- 每轮检查 Git diff；
- 每轮必须给出真实命令和结果；
- 架构变化必须创建 ADR；
- 未通过构建不得标记阶段完成。

------

# 十四、项目完成验收标准

项目达到第一版完成状态时，应满足：

1. 可以在纯血鸿蒙设备中安装并启用；
2. 输入法窗口可以稳定显示和隐藏；
3. 支持完整的小鹤双拼基础映射；
4. 能够输入单字、词语和短句；
5. 候选栏展示、点击和翻页正常；
6. 删除、空格、回车和模式切换正常；
7. 密码、数字、邮箱等输入框适配正常；
8. 用户词频能够本地保存并影响排序；
9. 清空用户词频后能够恢复默认排序；
10. ArkTS、C++、Rust 职责边界清晰；
11. C++ 中没有输入法业务算法；
12. ArkTS 中没有双拼解析和候选排序；
13. Rust 核心可以在桌面环境独立运行测试；
14. Native 内存没有明显泄漏；
15. 高频输入、应用切换和横竖屏切换不崩溃；
16. Release 构建不输出用户输入内容；
17. 所有第三方词库和依赖具有明确许可证；
18. README、架构文档、接口文档和测试文档完整；
19. 构建环境和版本已经固定；
20. 核心回归语料测试全部通过。

------

# 十五、最终工程约束总结

整个项目必须始终遵守：

```text
ArkTS 负责鸿蒙能力和交互
C++ 负责跨语言桥接
Rust 负责输入法核心算法

```

并且：

```text
ExtensionAbility 不堆 UI 逻辑
UI 组件不直接调用 Native
C++ 不实现业务算法
Rust 不调用鸿蒙系统接口
词库不在启动时解析原始文本
状态不散落在多个组件
错误不能只靠日志处理
阶段没有测试就不能标记完成
AI 没有真实执行就不能声称通过

```

第一项正式开发任务不是设计完整键盘，也不是导入大型词库，而是完成：

```text
输入法 ExtensionAbility
        ↓
ArkTS 调用 C++
        ↓
C++ 调用 Rust
        ↓
Rust 返回测试候选
        ↓
候选显示并提交到输入框

```

只有这一条最小链路在真机成功后，项目才进入正式功能开发。
